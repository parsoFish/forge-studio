---
kind: tutorial
story: S1
generated_from: tests/stories/S1.story.mjs
---

# Onboard an existing project

> Generated by `npm run stories -- --story S1` from `tests/stories/S1.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open Studio on the Projects pillar

The Projects pillar lists every project forge manages. GitWeave is already discovered from disk, but it needs attention: it has no contract yet, so no Flow can be pointed at it.

![Open Studio on the Projects pillar](frames/01-open-studio-on-the-projects-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `projects-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-card-id` is `gitweave`
- `data-health` is `attention`

</details>

## 2. Click "Onboard a project"

Onboarding asks for the few things a Factory needs before it can build a repo: what to call it, the quality gate that judges its work, and the north star that tells a planner what the project is for.

![Click "Onboard a project"](frames/02-click-onboard-a-project.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `new`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-section` is `project-onboard`
- `data-load-error` is `false`

</details>

## 3. Fill in the name, the quality gate and the north star — and under Advanced, the repo path — then press "Onboard project →"

Registering the project lands the operator on its page, where forge immediately measures GitWeave against the project contract and reports the result honestly: a hard fail. Registration scaffolds the contract artifacts forge can write without asking anyone, so the checklist says contract — present, and preflight says hard fail, and both are true at once: a scaffolded contract is a stub, not an answer. The secrets, demo and roadmap stages have nothing at all; only the instructions carry real content, read from the repo's own CLAUDE.md. No Flow can be pointed at a project in this state.

![Fill in the name, the quality gate and the north star — and under Advanced, the repo path — then press "Onboard project →"](frames/03-fill-in-the-name-the-quality-gate-and-th.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `gitweave`
- `data-page-ready` is `true`
- `data-checklist-row` is `contract`
- `data-checklist-status` is `present`
- `data-preflight-status` is `hard-fail`

</details>

## 4. Open "Brief the agent", give it the north star and the gate command, and press "Run onboarding agent"

The operator does not fill the contract in by hand. An Agent does it, briefed with the two things only the operator knows: what the project is for, and the command that tells the truth about whether it works.

![Open "Brief the agent", give it the north star and the gate command, and press "Run onboarding agent"](frames/04-open-brief-the-agent-give-it-the-north-s.png)

<details><summary>What you should see</summary>

- `data-section` is `onboard-with-agent`
- `data-onboard-run-status` is `running`
- `data-onboard-session-id` is `2026-09-05T02-06-32-703c9252`
- `data-onboard-attaching` is `false`

</details>

## 5. Follow "View onboarding session"

The onboarding session opens on the shared session surface. Its live artifact is the same five-stage Contract Buildout the project page shows — contract, instructions, secrets, demo, roadmap — so the operator watches the Agent close the gaps in the same vocabulary the gate will judge.

![Follow "View onboarding session"](frames/05-follow-view-onboarding-session.png)

<details><summary>What you should see</summary>

- `data-page` is `session`
- `data-page-ready` is `true`
- `data-session-kind` is `onboarding`
- `data-session-stage` is `contract`
- `data-buildout-mode` is `checklist`
- `data-buildout-row-count` is `5`

</details>

## 6. Answer the Agent's questions about the quality gate and what GitWeave must never touch, stage by stage, until the contract and secrets stages read present

This is the part only a human can supply. The Agent runs the contract criteria and invokes the Skills built for the purpose, but the decisions — what the done-signal is, which files are untouchable, which credentials the acceptance tier needs — are the operator's. The secrets stage names the environment variables and never their values.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not fill [data-field="session-answer"] with "The quality gate is `python -m pytest tests/`. Never touch infra/ state or config/orgs/*.yaml — GitWeave applies those to a real GitHub organisation.": locator.evaluate: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-field="session-answer"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Answer the Agent's questions about the quality gate and what GitWeave must never touch, stage by stage, until the contract and secrets stages read present](frames/06-answer-the-agent-s-questions-about-the-q.png)

<details><summary>What you should see</summary>

- `data-session-kind` is `onboarding`
- `data-session-stage` is `contract`

</details>

## 7. Select the demo stage and hand it to the demo builder — the heavy one

Not every contract component is a question and an answer. The demo process is a build in its own right, so it gets its own long-running session rather than blocking the onboarding one. Handing it over does not take the operator anywhere: the demo session is minted and named on the page they are standing on, so they can walk into it now or come back to it later.

![Select the demo stage and hand it to the demo builder — the heavy one](frames/07-select-the-demo-stage-and-hand-it-to-the.png)

<details><summary>What you should see</summary>

- `data-page` is `session`
- `data-page-ready` is `true`
- `data-session-kind` is `onboarding`
- `data-session-id` is `2026-09-05T02-06-32-703c9252`
- `data-stage-detail-stage` is `demo`

</details>

## 8. Come back to the demo builder when it has finished

The operator can leave a heavy session and come back — the session is the record, not the terminal it was started from. This is the beat that proves it: the demo builder was started from one surface, left alone, and read from another, and the work is there and finished.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/sessions/demo/2026-09-05T02-06-32-703c9252" from "/sessions/demo/2026-09-05T02-06-53-05028a45": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Come back to the demo builder when it has finished](frames/08-come-back-to-the-demo-builder-when-it-ha.png)

<details><summary>What you should see</summary>

- `data-page` is `session`
- `data-page-ready` is `true`
- `data-session-kind` is `demo`
- `data-session-phase` is `briefing`

</details>

## 9. Return to the project, apply the auto-fixable clauses and record a decision on the ones that need your judgement

Preflight is MET. GitWeave now has a contract forge can hold it to, and the project is Flow-ready: the gates downstream have something real to judge against.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="apply-clause-decision"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="apply-clause-decision"]').first()[22m
[2m    - locator resolved to <button disabled data-apply-clause-id="C1b" data-action="apply-clause-decision" title="Auto-fixes are applying — one clause at a time">Apply with agent</button>[22m
[2m  - attempting click action[22m
[2m    2 × waiting for element to be visible, enabled and stable[22m
[2m      - element is not enabled[22m
[2m    - retrying click action[22m
[2m    - waiting 20ms[22m
[2m    2 × waiting for element to be visible, enabled and stable[22m
[2m      - element is not enabled[22m
[2m    - retrying click action[22m
[2m      - waiting 100ms[22m
[2m    9 × waiting for element to be visible, enabled and stable[22m
[2m      - element is not enabled[22m
[2m    - retrying click action[22m
[2m      - waiting 500ms[22m
[2m    - waiting for element to be visible, enabled and stable[22m
. The control is absent, disabled, obscured or not yet rendered.

![Return to the project, apply the auto-fixable clauses and record a decision on the ones that need your judgement](frames/09-return-to-the-project-apply-the-auto-fix.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `gitweave`

</details>

## 10. Press "Plan with Architect" and describe the first piece of work

With a contract in place the Architect can plan. It interviews the operator, reads the project, and produces a roadmap for review — the first Gate a human stands at. A real Agent costs money, so the operator caps this run before starting it, and forge names the session it just minted on the page they are standing on rather than sweeping them into it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="plan-with-architect"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="plan-with-architect"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Press "Plan with Architect" and describe the first piece of work](frames/10-press-plan-with-architect-and-describe-t.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `gitweave`

</details>

## 11. Open the session, read the plan and press Approve

The plan is approved and committed. An existing repo that forge knew nothing about half an hour ago is now an onboarded project with a contract, a demo, a knowledge profile and an approved roadmap — ready for a Factory to build.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="view-architect-session"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="view-architect-session"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Open the session, read the plan and press Approve](frames/11-open-the-session-read-the-plan-and-press.png)
