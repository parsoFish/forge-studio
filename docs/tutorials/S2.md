---
kind: tutorial
story: S2
generated_from: tests/stories/S2.story.mjs
---

# Create a new project from scratch

> Generated by `npm run stories -- --story S2` from `tests/stories/S2.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open Studio on the Projects pillar

The Projects pillar lists every project forge manages. The tool this story is about is not here — it does not exist yet, on disk or anywhere else. That is the starting state: no repo, no contract, nothing to point a Flow at.

![Open Studio on the Projects pillar](frames/01-open-studio-on-the-projects-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `projects-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 2. Press "Start a greenfield project"

One page hosts both doors into forge: register a repo that already exists, or scaffold a new one. Three starters ship with forge, and the operator picks the one shaped like the thing they are about to build.

![Press "Start a greenfield project"](frames/02-press-start-a-greenfield-project.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `new`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-section` is `project-create`
- `data-app-type-count` is `3`

</details>

## 3. Name it, say what it is for, pick the "typescript-cli" starter, and press "Create project"

Forge writes the repo from the starter, makes the first commit in it, and lands the operator on the project page. Nothing linked here a moment ago — the route exists because the button was pressed.

![Name it, say what it is for, pick the "typescript-cli" starter, and press "Create project"](frames/03-name-it-say-what-it-is-for-pick-the-type.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `story-s2`
- `data-page-ready` is `true`

</details>

## 4. Check the project is ready for a Flow, and what it still owes

This is the promise a starter exists to keep, stated exactly. Onboarding an existing repo needs an Agent to fill the contract in, question by question; creating one from a starter should not, because forge wrote every file itself. So the operator checks the two things that decide it: preflight MET, and the project ready for a Flow. Ready does not mean finished. Forge wrote every file it could write from a template; what a template cannot write is a demo of THIS project, and the form says so rather than showing a green tick it has not earned.

![Check the project is ready for a Flow, and what it still owes](frames/04-check-the-project-is-ready-for-a-flow-an.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `story-s2`
- `data-preflight-status` is `ok`
- `data-flow-ready` is `true`

</details>

## 5. Look at what forge says is still unresolved, and who owes it

The panel that catches a half-onboarded repo is where the difference shows. Nothing here is waiting on the operator: the two open clauses are the demo skill and its alignment, and the contract says outright that an agent generates them. Creation does not promise a demo it has not run. Every clause a template can answer, the starter answered — and the two it cannot are named, owned and counted rather than quietly ticked.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-resolution-failing-count: expected "2", got "3"
> - data-resolution-user-count: expected "0", got "1"

![Look at what forge says is still unresolved, and who owes it](frames/05-look-at-what-forge-says-is-still-unresol.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `story-s2`
- `data-section` is `contract-resolution`
- `data-resolution-failing-count` is `3`
- `data-resolution-user-count` is `1`
- `data-resolution-agent-count` is `2`

</details>

## 6. Review the contract the starter filled in, element by element

This is the act that makes creating a project different from onboarding one. Onboarding fills a contract in, question by question, with an Agent. Creation hands the operator one that is already filled — contract, instructions, secrets, demo, roadmap — and asks them to read it. Five elements, every one already answered by the starter.

![Review the contract the starter filled in, element by element](frames/06-review-the-contract-the-starter-filled-i.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `story-s2`
- `data-section` is `contract-checklist`
- `data-checklist-row-count` is `5`

</details>

## 7. Read the secrets element — the one a starter cannot guess from a template

Secrets is the element a template has the least right to assume, so it is the one worth reading first. Present does not mean forge invented credentials: the element names the environment variables the acceptance tier will need, and never a value. If the starter guessed wrong, this is where the operator sees it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-checklist-row: expected "secrets", got "contract"

![Read the secrets element — the one a starter cannot guess from a template](frames/07-read-the-secrets-element-the-one-a-start.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `story-s2`
- `data-checklist-row` is `contract`
- `data-checklist-status` is `present`

</details>

## 8. Adjust the demo the starter wrote — this CLI needs a third step that runs the built binary — and save the project

Reviewing is only half of it. The starter wrote a demo good enough for any typescript CLI; this project measures build stages, so its demo has to run the built binary against a fixture and show the timings. The operator changes it here and saves, and the contract the gates judge against is the one they just read.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-step-count: expected "3", got "2"

![Adjust the demo the starter wrote — this CLI needs a third step that runs the built binary — and save the project](frames/08-adjust-the-demo-the-starter-wrote-this-c.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `story-s2`
- `data-step-count` is `2`

</details>

## 9. Press "Architect →"

With a contract in place the Architect can plan. Studio carries the new project through, so the only thing still missing is the one thing forge cannot know: what the operator wants built first.

![Press "Architect →"](frames/09-press-architect.png)

<details><summary>What you should see</summary>

- `data-page` is `architect-new`
- `data-page-ready` is `true`
- `data-section` is `new-idea`
- `data-roster-state` is `ok`
- `data-new-idea-ready` is `false`

</details>

## 10. Describe the first piece of work, cap what this run may spend, and press "Start architect"

The Architect is a real Agent and a real Agent costs money, so the operator sets this run's ceiling before starting it rather than discovering the bill afterwards. Pressing Start mints a session and shows its id on the page the operator is standing on — the run exists, and it is nameable, before anything navigates anywhere.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-architect-session-id: expected a value to bind as <architectSessionId>, got ""

![Describe the first piece of work, cap what this run may spend, and press "Start architect"](frames/10-describe-the-first-piece-of-work-cap-wha.png)

<details><summary>What you should see</summary>

- `data-page` is `architect-new`
- `data-section` is `new-idea`
- `data-architect-session-id` is ``

</details>

## 11. While it works, check Monitor

Monitor is the one surface that answers "what is running, and what is stuck". The architect run is on it — a Flow run against a project that did not exist a few minutes ago, which is the whole point of S2.

![While it works, check Monitor](frames/11-while-it-works-check-monitor.png)

<details><summary>What you should see</summary>

- `data-page` is `monitor`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-session-kind` is `architect`

</details>

## 12. Open the session again and answer the Architect's questions about the project

The Architect interviews before it plans. The operator supplies the context only they have — what "done" means, what must not break — and the session moves to awaiting a verdict once the plan is drafted.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/architect/<architectSessionId>" needs <architectSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Open the session again and answer the Architect's questions about the project](frames/12-open-the-session-again-and-answer-the-ar.png)

## 13. Open the plan and stand at the gate

This is the plan gate — the first Gate a human stands at, and where S2 ends. A starter became a repo, the repo became a contract-ready project, and the first Flow run on it reached the gate with a plan waiting on a decision. Nothing is approved yet: that decision is the operator's, and S10 is the story that makes it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="open-plan"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="open-plan"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Open the plan and stand at the gate](frames/13-open-the-plan-and-stand-at-the-gate.png)

<details><summary>What you should see</summary>

- `data-fetch-status` is `ok`

</details>
