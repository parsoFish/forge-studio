---
kind: how-to
story: S8
generated_from: tests/stories/S8.story.mjs
---

# Install library components from the community

> Generated by `npm run stories -- --story S8` from `tests/stories/S8.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open the Library and look at the shelf forge did not write

Four of the Library’s shelves hold parts forge ships. The fifth holds what other people published, and nothing on it is installed — it is a catalogue, not an inventory.

![Open the Library and look at the shelf forge did not write](frames/01-open-the-library-and-look-at-the-shelf-f.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `community`
- `data-count` is `20`

</details>

## 2. Press "Browse community"

One browser over four kinds — skills, hooks, MCPs and tools. It owns no trust decision at all: everything here either installs through the page that owns it, or says exactly why it cannot.

![Press "Browse community"](frames/02-press-browse-community.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`
- `data-item-count` is `20`
- `data-hub-count` is `9`
- `data-kind-filter` is `all`
- `data-last-refresh` is `never`

</details>

## 3. Read where the twenty items actually come from

Nine sources are declared and four of them contribute nothing. The count on each chip is derived per request, never claimed — so the strip is forge admitting, in the DOM, that its list of what the community publishes is a hand-curated seed rather than the sources’ own contents.

![Read where the twenty items actually come from](frames/03-read-where-the-twenty-items-actually-com.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`
- `data-hub-count` is `9`
- `data-action` is `filter-hub`
- `data-hub-id` is `skills-sh`
- `data-hub-item-count` is `0`
- `data-hub-declared-only` is `true`

</details>

## 4. Refresh the registry against those sources

forge crawls nothing on its own — a refresh happens exactly when an operator asks for one, and this is the ask. It is deterministic and LLM-free: real outbound calls to the hubs, no agent turn, no verdict step, no spend.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-refresh-state: expected "refreshed", got "refused"

![Refresh the registry against those sources](frames/04-refresh-the-registry-against-those-sourc.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`
- `data-section` is `refresh-result`
- `data-refresh-state` is `refused`

</details>

## 5. Check that every declared source now contributes what it publishes

This is what "browse the registry" has to mean: the list the operator browses is what the declared sources actually publish, not the subset somebody typed into a yaml file. A source that stays at zero after a refresh is a source forge has never really consumed.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-hub-id: expected "skills-sh", got "superpowers"

![Check that every declared source now contributes what it publishes](frames/05-check-that-every-declared-source-now-con.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`
- `data-hub-count` is `9`
- `data-action` is `filter-hub`
- `data-hub-id` is `superpowers`
- `data-hub-declared-only` is `false`

</details>

## 6. Check that no row is still a hand-curated seed

Every row currently reads "seed — never verified", which is the truth and is exactly the problem. A registry the operator is about to install executable code from should be able to say when it last checked.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-freshness: expected "fresh", got "seed"

![Check that no row is still a hand-curated seed](frames/06-check-that-no-row-is-still-a-hand-curate.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`
- `data-component` is `freshness-badge`
- `data-freshness` is `seed`

</details>

## 7. Take the first door in — press "+ Add item" to install by URL

The registry is a declared list, and this is how the operator declares something into it. Curation is not trust: adding a row says what is browsable, never what is safe to run.

![Take the first door in — press "+ Add item" to install by URL](frames/07-take-the-first-door-in-press-add-item-to.png)

<details><summary>What you should see</summary>

- `data-page` is `community-registry-form`
- `data-form-mode` is `add`
- `data-page-ready` is `true`

</details>

## 8. Give forge nothing but the upstream URL and what to call it

Star counts and last-checked dates are not on this form on purpose — they are facts a fetch establishes, not claims an operator types. A row added here reads "seed — never verified" until a refresh actually checks it.

![Give forge nothing but the upstream URL and what to call it](frames/08-give-forge-nothing-but-the-upstream-url-.png)

<details><summary>What you should see</summary>

- `data-page` is `community-detail`
- `data-item-id` is `story-s8-skill`
- `data-page-ready` is `true`
- `data-item-kind` is `skill`
- `data-install-state` is `not-installed`

</details>

## 9. Install the thing that URL names

This is where §3’s two paths turn out to be one. forge can install what it has already vendored into its own repo; for everything else the answer is a link to somebody else’s website. Adding a row by URL makes an item browsable and never makes it installable.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-install-action: expected "install", got "browse-upstream"

![Install the thing that URL names](frames/09-install-the-thing-that-url-names.png)

<details><summary>What you should see</summary>

- `data-page` is `community-detail`
- `data-item-id` is `story-s8-skill`
- `data-page-ready` is `true`
- `data-section` is `install`
- `data-install-action` is `browse-upstream`

</details>

## 10. Go back to the browser

Back to the catalogue, to take the door that does end in an install.

![Go back to the browser](frames/10-go-back-to-the-browser.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`

</details>

## 11. Find the one item in the registry that is executable code

Twenty items and exactly one of them is a script. Skills, MCP entries and tools are instructions and addresses; a hook is code that will run with the operator’s own credentials, which is why the next two beats exist and the others do not need them.

![Find the one item in the registry that is executable code](frames/11-find-the-one-item-in-the-registry-that-i.png)

<details><summary>What you should see</summary>

- `data-page` is `community-browser`
- `data-page-ready` is `true`
- `data-item-count` is `1`
- `data-card-type` is `community-item`
- `data-item-id` is `block-protected-branch-push`
- `data-item-kind` is `hook`
- `data-item-hub` is `forge-seed`
- `data-install-state` is `not-installed`

</details>

## 12. Open it and read every file in the package before trusting any of it

Two files: the manifest that declares what it runs on and what it may touch, and the script itself. Both are readable here, before anything is written to disk.

![Open it and read every file in the package before trusting any of it](frames/12-open-it-and-read-every-file-in-the-packa.png)

<details><summary>What you should see</summary>

- `data-page` is `community-detail`
- `data-item-id` is `block-protected-branch-push`
- `data-page-ready` is `true`
- `data-item-kind` is `hook`
- `data-install-state` is `not-installed`
- `data-component` is `file-package`
- `data-file-count` is `2`
- `data-active-file` is `hook.yaml`

</details>

## 13. Read the security scan that runs before the install exists

The order is the whole point: scanned before installed, not installed then scanned. An operator who has to install something to find out whether it is safe has already run it.

![Read the security scan that runs before the install exists](frames/13-read-the-security-scan-that-runs-before-.png)

<details><summary>What you should see</summary>

- `data-page` is `community-detail`
- `data-item-id` is `block-protected-branch-push`
- `data-page-ready` is `true`
- `data-section` is `security-scan`
- `data-scan-verdict` is `clean`
- `data-finding-count` is `0`
- `data-critical-count` is `0`

</details>

## 14. Take the second door in — install it by id

Installed, and trusted by nobody. The browser hands the operator straight to the page that owns the decision it deliberately cannot make here.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-install-state: expected "needs-review", got "not-installed"
> - data-install-action: expected "open-owning", got "install"

![Take the second door in — install it by id](frames/14-take-the-second-door-in-install-it-by-id.png)

<details><summary>What you should see</summary>

- `data-page` is `community-detail`
- `data-item-id` is `block-protected-branch-push`
- `data-page-ready` is `true`
- `data-install-state` is `not-installed`
- `data-section` is `install`
- `data-install-action` is `install`

</details>

## 15. Follow it to the page that owns the trust decision

A hook that came from a stranger arrives in the same state as one the operator wrote by hand: on disk, described, scanned, and refused permission to run. Provenance buys it nothing.

![Follow it to the page that owns the trust decision](frames/15-follow-it-to-the-page-that-owns-the-trus.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-detail`
- `data-hook-id` is `block-protected-branch-push`
- `data-page-ready` is `true`
- `data-hook-event` is `PreToolUse`
- `data-hook-trust` is `needs-review`
- `data-hook-runnable` is `false`

</details>

## 16. Approve it

One press, and a file somebody else wrote becomes something forge will execute. That is the whole reason every beat since the search box was about reading rather than clicking.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-hook-trust: expected "approved", got "needs-review"
> - data-hook-runnable: expected "true", got "false"

![Approve it](frames/16-approve-it.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-detail`
- `data-hook-id` is `block-protected-branch-push`
- `data-page-ready` is `true`
- `data-hook-trust` is `needs-review`
- `data-hook-runnable` is `false`

</details>

## 17. Open the Agents pillar

A library part is inert until something composes it. The last question this story asks is whether the thing the operator just approved is now available to build with.

![Open the Agents pillar](frames/17-open-the-agents-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `agents-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 18. Find the installed hook in the palette an agent composes from

This is where S8 ends and where a community install becomes real: not when the bytes land, and not when the operator approves them, but when the thing shows up in the bin an agent is built out of. Anything short of that is a download.

![Find the installed hook in the palette an agent composes from](frames/18-find-the-installed-hook-in-the-palette-a.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-page-ready` is `true`
- `data-agent-id` is `brain-ingest`
- `data-id` is `block-protected-branch-push`
- `data-kind` is `hook`

</details>
