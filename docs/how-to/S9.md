---
kind: how-to
story: S9
generated_from: tests/stories/S9.story.mjs
---

# Drive forge through the assistant

> Generated by `npm run stories -- --story S9` from `tests/stories/S9.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open Studio

Nothing is running. Every one of S1 through S8 is something the operator can do with their own hands from here; this story hands the same work to the assistant instead, and the difference has to be visible in what it costs.

![Open Studio](frames/01-open-studio.png)

<details><summary>What you should see</summary>

- `data-page` is `home`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-section` is `sessions-needing-you`
- `data-active-session-count` is `0`

</details>

## 2. Press "View all sessions"

This is the spine, and it is S9’s whole subject: seven doors, one per session kind, and behind them the assistant-driven variant of most of the other eight stories. `fetch-status` is here on purpose — an empty list and an unreachable bridge are different facts and this page refuses to render them the same.

![Press "View all sessions"](frames/02-press-view-all-sessions.png)

<details><summary>What you should see</summary>

- `data-page` is `sessions-index`
- `data-page-ready` is `true`
- `data-session-count` is `0`
- `data-fetch-status` is `ok`

</details>

## 3. Ask the assistant to do S7’s job — author a library component

Same work as S7, different hands. The operator is not going to write the package, name its directory or check its frontmatter — they are going to say what they want and pay for a turn.

![Ask the assistant to do S7’s job — author a library component](frames/03-ask-the-assistant-to-do-s7-s-job-author-.png)

<details><summary>What you should see</summary>

- `data-page` is `session-kickoff`
- `data-page-ready` is `true`
- `data-kickoff-kind` is `authoring`

</details>

## 4. Point it at a project and tell it what to build

The brief is the whole instruction — there is no second turn where the operator fills in what they meant. Every other kickoff kind takes its brief later; this one takes it here, because the agent has nothing else to go on.

![Point it at a project and tell it what to build](frames/04-point-it-at-a-project-and-tell-it-what-t.png)

<details><summary>What you should see</summary>

- `data-page` is `session-kickoff`
- `data-page-ready` is `true`
- `data-kickoff-kind` is `authoring`
- `data-action` is `start-session`
- `data-existing-count` is `0`
- `data-confirming` is `false`

</details>

## 5. Set the SDK, the model and the effort this session will run on

Three knobs is what §3 promises and one is what the page offers. Which model a turn runs on is the cheapest of the three to get right, and it is the only one an operator can touch — the SDK the session runs under and how hard it is allowed to think are decided somewhere the operator never sees.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not fill [data-field="kickoff-model-tier-option"] with "opus": locator.fill: Error: Input of type "radio" cannot be filled
Call log:
[2m  - waiting for locator('[data-field="kickoff-model-tier-option"]').first()[22m
[2m    - locator resolved to <label data-field="kickoff-model-tier-option">…</label>[22m
[2m    - fill("opus")[22m
[2m  - attempting fill action[22m
[2m    - waiting for element to be visible, enabled and editable[22m
. The control is absent, disabled, obscured or not yet rendered.

![Set the SDK, the model and the effort this session will run on](frames/05-set-the-sdk-the-model-and-the-effort-thi.png)

<details><summary>What you should see</summary>

- `data-page` is `session-kickoff`
- `data-page-ready` is `true`
- `data-kickoff-kind` is `authoring`

</details>

## 6. Start it

From here the assistant is doing S7’s work: reading the project, drafting the package, and stopping at the approval S7’s operator would have reached by typing. The operator’s job for the next few minutes is to watch what it costs.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/authoring/<authoringSessionId>" needs <authoringSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Start it](frames/06-start-it.png)

## 7. Check the session is running on the model the operator chose

This is the one part of the promise that already works end to end: a choice made on the kickoff form is a fact the running session states about itself, not a hope. The other two knobs have nothing to state because nothing set them.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/authoring/<authoringSessionId>" needs <authoringSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Check the session is running on the model the operator chose](frames/07-check-the-session-is-running-on-the-mode.png)

## 8. Read what the session has cost so far

A session the operator is paying for by the token should be able to say what it has spent, while it is spending it. This is the beat that decides whether "cost is recorded" means anything, and the operator asking the question has nowhere to look.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/authoring/<authoringSessionId>" needs <authoringSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Read what the session has cost so far](frames/08-read-what-the-session-has-cost-so-far.png)

## 9. Go back to Studio’s front page

One assistant working, counted in the one place the operator looks first. Beat 1 pinned this at zero for exactly this comparison.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-active-session-count: expected "1", got "0"

![Go back to Studio’s front page](frames/09-go-back-to-studio-s-front-page.png)

<details><summary>What you should see</summary>

- `data-page` is `home`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-section` is `sessions-needing-you`
- `data-active-session-count` is `0`

</details>

## 10. Press "View all sessions" and find it on the spine

The spine lists what it started. Kind, project, phase, state, model tier, last update — six columns, and not one of them is money.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-session-count: expected "1", got "0"
> - data-session-kind: expected "authoring", absent from the page
> - data-session-state: expected "working", absent from the page
> - data-needs-you: expected "false", absent from the page

![Press "View all sessions" and find it on the spine](frames/10-press-view-all-sessions-and-find-it-on-t.png)

<details><summary>What you should see</summary>

- `data-page` is `sessions-index`
- `data-page-ready` is `true`
- `data-session-count` is `0`
- `data-fetch-status` is `ok`

</details>

## 11. Ask the assistant to do S1’s job, and choose ITS model too

Second variant, and the promise comes apart on it. One kind lets the operator pick a model and the next fixes it read-only — same spine, same form, same words on the page, opposite amount of control. "Per session" has to mean per session, or the operator has to know which kinds it is untrue for.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-model-tier-picker: expected "range", got "fixed"

![Ask the assistant to do S1’s job, and choose ITS model too](frames/11-ask-the-assistant-to-do-s1-s-job-and-cho.png)

<details><summary>What you should see</summary>

- `data-page` is `session-kickoff`
- `data-page-ready` is `true`
- `data-kickoff-kind` is `onboarding`
- `data-section` is `kickoff-model-tier`
- `data-model-tier-picker` is `fixed`

</details>

## 12. Start it and let both of them run

Two assistants now, on two different jobs, one of which the operator chose a model for and one of which chose for itself. Whatever else is true, they are both spending real money right now.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/onboarding/<onboardingSessionId>" needs <onboardingSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Start it and let both of them run](frames/12-start-it-and-let-both-of-them-run.png)

## 13. Open the Monitor and read what the two of them cost

The Monitor is where forge shows what it has spent, and it is honest about knowing nothing — an empty ledger, not a fabricated zero row. It is also the only place an operator could look, so a kind of work that never reaches it is a kind of work that costs an unknown amount forever.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-ledger-total: expected "2", got "0"
> - data-ledger-count: expected "2", got "0"

![Open the Monitor and read what the two of them cost](frames/13-open-the-monitor-and-read-what-the-two-o.png)

<details><summary>What you should see</summary>

- `data-page` is `monitor`
- `data-page-ready` is `true`
- `data-ledger-total` is `0`
- `data-fetch-status` is `ok`
- `data-section` is `history-ledger`
- `data-ledger-count` is `0`

</details>

## 14. Read the authoring session’s own figure, against the ceiling it was given

This is where S9 ends, on the one claim that makes the other eight stories affordable to run through an assistant at all: that the operator can see, per session, what they were charged for handing the work over. Until they can, "ask the assistant to do it" is a bill with no itemisation.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-ledger-agent: expected "creation-agent", absent from the page
> - data-ledger-cost-usd: expected "<authoringCostUsd>", absent from the page

![Read the authoring session’s own figure, against the ceiling it was given](frames/14-read-the-authoring-session-s-own-figure-.png)

<details><summary>What you should see</summary>

- `data-page` is `monitor`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>
