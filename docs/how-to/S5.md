---
kind: how-to
story: S5
generated_from: tests/stories/S5.story.mjs
---

# Create a new agent

> Generated by `npm run stories -- --story S5` from `tests/stories/S5.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open Studio on the Agents pillar

The Agents pillar lists every agent forge can dispatch. They are the workers a Flow puts at its stations, and the operator is about to add one that does not exist yet.

![Open Studio on the Agents pillar](frames/01-open-studio-on-the-agents-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `agents-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-section` is `agent-roster`

</details>

## 2. Press "+ New agent"

A new agent starts from a starter — a ready-made shape the operator edits — or from nothing at all. The empty `agent-id` is the page saying, honestly, that nothing has been minted yet.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not click through to "/agents/new" from "/agents/new": locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('a[href="/agents/new"]').first()[22m
[2m    - locator resolved to <a href="/agents/new" class="btn btn-primary" data-action="new-agent">+ New agent</a>[22m
[2m  - attempting click action[22m
[2m    - waiting for element to be visible, enabled and stable[22m
[2m  - element was detached from the DOM, retrying[22m
. The control exists but was not actionable — obscured, disabled or detached.

![Press "+ New agent"](frames/02-press-new-agent.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``
- `data-page-ready` is `true`

</details>

## 3. Start from the Blank agent starter, name it "story S5", and say what it exists to do

Every agent needs one sentence that says why it exists, because that sentence is what a planner reads when it decides which agent to put at a station. This one is: “Read a project’s own docs and its live pages, and report every place the docs describe a page that no longer exists.”

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-ok: expected "true", got "false"

![Start from the Blank agent starter, name it "story S5", and say what it exists to do](frames/03-start-from-the-blank-agent-starter-name-.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``
- `data-check` is `purpose`
- `data-ok` is `false`

</details>

## 4. Open Advanced and compose the two skills this agent needs

Composing is the whole idea: an agent is not written, it is assembled out of skills the library already holds. This one takes brain-query and webapp-testing — one to look up what the project already knows, one to drive its pages.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="toggle-advanced"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="toggle-advanced"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Open Advanced and compose the two skills this agent needs](frames/04-open-advanced-and-compose-the-two-skills.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`

</details>

## 5. Set the tool fence: this agent may invoke git and Node, and nothing else

The fence is the point of the Advanced drawer. An agent reaches exactly the tools it is given and no others, so the operator states the list rather than discovering it from a transcript later.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-accepts: expected "tool", absent from the page
> - data-count: expected "2", absent from the page

![Set the tool fence: this agent may invoke git and Node, and nothing else](frames/05-set-the-tool-fence-this-agent-may-invoke.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`

</details>

## 6. Leave the MCP zone empty — the fence is a closed list, not a starting point

This agent talks to a repo and a browser. It has no reason to hold a memory graph or a database connection, so it gets neither. A fence that only ever grows is not a fence.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-accepts: expected "mcp", absent from the page
> - data-count: expected "0", absent from the page

![Leave the MCP zone empty — the fence is a closed list, not a starting point](frames/06-leave-the-mcp-zone-empty-the-fence-is-a-.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`

</details>

## 7. Save the agent

Saving mints the agent and lands the operator on its own page. Nothing linked here a moment ago — the route exists because the button was pressed.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="save-agent"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="save-agent"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Save the agent](frames/07-save-the-agent.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``
- `data-page-ready` is `true`

</details>

## 8. Check the agent is ready — all six readiness checks pass

Readiness is forge refusing to pretend. Six checks, each of which can genuinely fail, and an agent that misses one is an agent that will disappoint a station at three in the morning.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/agents/story-s5" from "/agents/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Check the agent is ready — all six readiness checks pass](frames/08-check-the-agent-is-ready-all-six-readine.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``

</details>

## 9. Cap what this run may spend at $2

A real agent costs real money, so the operator sets the cap before starting the run rather than discovering the bill afterwards — and the button says the figure back to them.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/agents/story-s5" from "/agents/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Cap what this run may spend at $2](frames/09-cap-what-this-run-may-spend-at-2.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``

</details>

## 10. Run the agent standalone against mdtoc

Standalone is an agent off the graph: no Flow, no station, just this agent against this project because the operator asked. It is how you find out whether the thing you assembled actually works before you wire it into a pipeline.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="run-agent"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="run-agent"]').first()[22m
[2m    - locator resolved to <button disabled data-run-ceiling="" class="btn btn-primary" data-action="run-agent" title="Save the agent (no unsaved changes) to run it" data-disabled-reason="Save the agent (no unsaved changes) to run it">Run agent (no cost cap)</button>[22m
[2m  - attempting click action[22m
[2m    2 × waiting for element to be visible, enabled and stable[22m
[2m      - element is not enabled[22m
[2m    - retrying click action[22m
[2m    - waiting 20ms[22m
[2m    2 × waiting for element to be visible, enabled and stable[22m
[2m      - element is not enabled[22m
[2m    - retrying click action[22m
[2m      - waiting 100ms[22m
[2m    10 × waiting for element to be visible, enabled and stable[22m
[2m       - element is not enabled[22m
[2m     - retrying click action[22m
[2m       - waiting 500ms[22m
. The control is absent, disabled, obscured or not yet rendered.

![Run the agent standalone against mdtoc](frames/10-run-the-agent-standalone-against-mdtoc.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``

</details>

## 11. Watch the run land in the agent’s own history

Every dispatch this agent has ever had is on its own page. One run, one row — the operator never has to go looking through a global log to find out what their own agent has been doing.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/agents/story-s5" from "/agents/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Watch the run land in the agent’s own history](frames/11-watch-the-run-land-in-the-agent-s-own-hi.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``

</details>

## 12. Read the row: what it cost, where it ran, and how it ended

This is where S5 ends, and the word that matters is honest. The row says the run was a standalone dispatch, that it finished, and what it actually cost — not a zero standing in for a figure nobody recorded. An agent whose page lies about its spend is an agent the operator cannot budget for.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/agents/story-s5" from "/agents/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Read the row: what it cost, where it ran, and how it ended](frames/12-read-the-row-what-it-cost-where-it-ran-a.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``

</details>
