---
kind: how-to
story: S5
generated_from: tests/stories/S5.story.mjs
---

# Create a new agent

> Generated by `npm run stories -- --story S5` from `tests/stories/S5.story.mjs`. Do not hand-edit — edit the story and re-run it.

## 1. Open Studio on the Agents pillar

The Agents pillar lists every agent forge can dispatch. They are the workers a Flow puts at its stations, and the operator is about to add one that does not exist yet.

![Open Studio on the Agents pillar](frames/01-open-studio-on-the-agents-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `agents-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-section` is `agent-roster`

</details>

## 2. Press "+ New agent"

A new agent starts from a starter — a ready-made shape the operator edits — or from nothing at all. The empty `agent-id` is the page saying, honestly, that nothing has been minted yet.

![Press "+ New agent"](frames/02-press-new-agent.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``
- `data-page-ready` is `true`
- `data-section` is `starter-picker`

</details>

## 3. Start from the Blank agent starter, name it "story S5", and say what it exists to do

Every agent needs one sentence that says why it exists, because that sentence is what a planner reads when it decides which agent to put at a station. This one is: “Read a project’s own docs and its live pages, and report every place the docs describe a page that no longer exists.”

![Start from the Blank agent starter, name it "story S5", and say what it exists to do](frames/03-start-from-the-blank-agent-starter-name-.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``
- `data-check` is `purpose`
- `data-ok` is `true`

</details>

## 4. Write the instructions: what this agent does, step by step

The purpose says why this agent exists; the instructions say how it works, and forge hands that body to the agent as its brief at dispatch time. An agent with no instructions is an agent told to improvise, which is why readiness refuses to pass without one — the Generate draft button beside the field is an offer, not a substitute.

![Write the instructions: what this agent does, step by step](frames/04-write-the-instructions-what-this-agent-d.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is ``
- `data-check` is `process`
- `data-ok` is `true`

</details>

## 5. Open Advanced and compose the two skills this agent needs

Composing is the whole idea: an agent is not written, it is assembled out of skills the library already holds. This one takes brain-query and webapp-testing — one to look up what the project already knows, one to drive its pages.

![Open Advanced and compose the two skills this agent needs](frames/05-open-advanced-and-compose-the-two-skills.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-accepts` is `skill`
- `data-count` is `2`

</details>

## 6. Set the tool fence: this agent may invoke git and Node, and nothing else

The fence is the point of the Advanced drawer. An agent reaches exactly the tools it is given and no others, so the operator states the list rather than discovering it from a transcript later.

![Set the tool fence: this agent may invoke git and Node, and nothing else](frames/06-set-the-tool-fence-this-agent-may-invoke.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-accepts` is `tool`
- `data-count` is `2`

</details>

## 7. Leave the MCP zone empty — the fence is a closed list, not a starting point

This agent talks to a repo and a browser. It has no reason to hold a memory graph or a database connection, so it gets neither. A fence that only ever grows is not a fence.

![Leave the MCP zone empty — the fence is a closed list, not a starting point](frames/07-leave-the-mcp-zone-empty-the-fence-is-a-.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-accepts` is `mcp`
- `data-count` is `0`

</details>

## 8. Save the agent

Saving mints the agent and lands the operator on its own page. Nothing linked here a moment ago — the route exists because the button was pressed.

![Save the agent](frames/08-save-the-agent.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `story-s5`
- `data-page-ready` is `true`

</details>

## 9. Check the agent is ready — every readiness check the panel names

Readiness is forge refusing to pretend. Every check on that panel can genuinely fail, and an agent that misses one is an agent that will disappoint a station at three in the morning.

![Check the agent is ready — every readiness check the panel names](frames/09-check-the-agent-is-ready-every-readiness.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `story-s5`
- `data-check` is `connections`
- `data-check-state` is `ready`
- `data-ready-total` is `7`

</details>

## 10. Cap what this run may spend at $2

A real agent costs real money, so the operator sets the cap before starting the run rather than discovering the bill afterwards — and the button says the figure back to them.

![Cap what this run may spend at $2](frames/10-cap-what-this-run-may-spend-at-2.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `story-s5`
- `data-run-ceiling` is `2`

</details>

## 11. Run the agent standalone against mdtoc

Standalone is an agent off the graph: no Flow, no station, just this agent against this project because the operator asked. It is how you find out whether the thing you assembled actually works before you wire it into a pipeline.

![Run the agent standalone against mdtoc](frames/11-run-the-agent-standalone-against-mdtoc.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `story-s5`
- `data-run-status` is `running`
- `data-run-id` is `_agent-story-s5-2026-09-12T08-21-56-510-ul1h`

</details>

## 12. Watch the run land in the agent’s own history

Every dispatch this agent has ever had is on its own page. One run, one row — the operator never has to go looking through a global log to find out what their own agent has been doing.

![Watch the run land in the agent’s own history](frames/12-watch-the-run-land-in-the-agent-s-own-hi.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `story-s5`
- `data-ledger-count` is `1`

</details>

## 13. Read the row: what it cost, where it ran, and how it ended

This is where S5 ends, and the word that matters is honest. The row says the run was a standalone dispatch, that it finished, and what it actually cost — not a zero standing in for a figure nobody recorded. An agent whose page lies about its spend is an agent the operator cannot budget for.

![Read the row: what it cost, where it ran, and how it ended](frames/13-read-the-row-what-it-cost-where-it-ran-a.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `story-s5`
- `data-run-status` is `done`
- `data-ledger-link-kind` is `standalone`
- `data-ledger-cost-usd` is `0.33`

</details>
