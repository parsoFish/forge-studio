---
kind: how-to
story: S3
generated_from: tests/stories/S3.story.mjs
---

# Reset a project contract

> Generated by `npm run stories -- --story S3` from `tests/stories/S3.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open Studio on the Projects pillar

The Projects pillar lists every project forge manages. The one this story is about has been here longest, and the pillar says it is healthy — which is true of the repo and no longer true of the contract forge builds against it.

![Open Studio on the Projects pillar](frames/01-open-studio-on-the-projects-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `projects-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-card-id` is `terraform-provider-betterado`
- `data-health` is `healthy`

</details>

## 2. Open the project

The project page is where the contract lives: what forge tests with, what it demos with, what it releases with, and which skills its agents may reach for.

![Open the project](frames/02-open-the-project.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-page-ready` is `true`

</details>

## 3. Count the skills this project binds

Nine skills — an API explorer, a resource scaffolder, a docs generator, an acceptance-test author. They are the reason this project can be built unattended at all: they are the moves that are specific to an Azure DevOps provider and to nothing else forge manages.

![Count the skills this project binds](frames/03-count-the-skills-this-project-binds.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-accepts` is `skill`
- `data-count` is `9`

</details>

## 4. Read the first of them

It does not resolve. Nor do the other eight. The skills are on disk — they sit where this project was told to put them when it was onboarded, and the contract every project written since puts them somewhere else. Nothing is broken enough to fail a gate, which is exactly why it has gone unnoticed: the project is drifting away from forge, one convention at a time.

![Read the first of them](frames/04-read-the-first-of-them.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-kind` is `skill`
- `data-skill-id` is `ado-api-explorer`
- `data-resolved` is `missing`
- `data-skill-source` is `missing`

</details>

## 5. Press "Rebuild contract", and read the drift report it produces before applying it

Rebuilding is not repairing. Forge regenerates the mechanisms it owns — the project config, the test, demo and release processes, the skill wiring — from the template every project created today is built from, and it shows the operator a drift report first: what it will change, what it will leave alone, and what it cannot decide. Only then does it rewrite anything. Applying it leaves preflight MET on a project whose north star, instructions and secrets forge never had the right to touch — which is what the next four beats go and check, one at a time.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-section: expected "contract-drift-applied", got "start-work"
> - data-preflight-ok: expected "true", absent from the page

![Press "Rebuild contract", and read the drift report it produces before applying it](frames/05-press-rebuild-contract-and-read-the-drif.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-section` is `start-work`

</details>

## 6. Check the nine skills resolve now

The rebuild moved the skills to where the current contract expects them and rewrote the wiring that points at them. The bindings the project has carried all along now resolve, and the agents that run against this project can reach the moves that are specific to it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-resolved: expected "ok", got "missing"
> - data-skill-source: expected "project", got "missing"

![Check the nine skills resolve now](frames/06-check-the-nine-skills-resolve-now.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-kind` is `skill`
- `data-skill-id` is `ado-api-explorer`
- `data-resolved` is `missing`
- `data-skill-source` is `missing`

</details>

## 7. Check the secrets the rebuild had no right to touch

This is the first of the three things a rebuild must never regenerate. The contract names the environment variables the live acceptance tier needs — never a value — and a template cannot know that this project talks to Azure DevOps with a personal access token. The element still names it, so the acceptance gate still knows what it needs.

![Check the secrets the rebuild had no right to touch](frames/07-check-the-secrets-the-rebuild-had-no-rig.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-detail-line` is `AZDO_PERSONAL_ACCESS_TOKEN`

</details>

## 8. Check the instructions still point at the project’s own file

The second. This project keeps its conventions in AGENTS.md and forge binds to that file read-only; a rebuild that swapped it for the template’s own choice would silently retarget every agent that reads it. The source is unchanged, so the instructions the operator has been curating for months are still the ones in force.

![Check the instructions still point at the project’s own file](frames/08-check-the-instructions-still-point-at-th.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-contract-conventions-source` is `AGENTS.md`

</details>

## 9. Check the north star survived

The third. "Feature complete ADO provider that has data and resources defined for all resources available in the ADO API" is the sentence every planner reads before it decides what to build next. It is the one thing in the contract that is a judgement rather than a mechanism, and a rebuild that regenerated it would quietly change what forge is building.

![Check the north star survived](frames/09-check-the-north-star-survived.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-contract-northstar-state` is `present`

</details>

## 10. Confirm preflight is MET and the project is still ready for a Flow

This is the check that says the reset was worth doing rather than merely survivable. Five readiness elements met, preflight MET, and the project still ready to have a Flow pointed at it — the same bar a project created today has to clear, now cleared by one that predates it.

![Confirm preflight is MET and the project is still ready for a Flow](frames/10-confirm-preflight-is-met-and-the-project.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-preflight-status` is `ok`
- `data-flow-ready` is `true`
- `data-ready-count` is `5`

</details>

## 11. Run the onboarding agent to re-derive the parts a template cannot write, briefed with the contract’s own north star and gate

A template can regenerate the mechanisms forge owns. It cannot write an API explorer for Azure DevOps, so the skills the rebuild rewired still have to be re-derived against what this provider actually does. That is an Agent’s job, and it is briefed with what the contract already says rather than with anything new — the rebuild preserved both, so the operator is handing back the project’s own words.

![Run the onboarding agent to re-derive the parts a template cannot write, briefed with the contract’s own north star and gate](frames/11-run-the-onboarding-agent-to-re-derive-th.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `terraform-provider-betterado`
- `data-section` is `onboard-with-agent`
- `data-onboard-run-status` is `running`
- `data-onboard-session-id` is `2026-09-05T02-14-24-f40ef3dd`
- `data-onboard-attaching` is `false`

</details>

## 12. Follow "View onboarding session" and watch it work

The session opens on the shared session surface, showing the same five-element contract buildout the project page shows — so the operator watches the Agent finish the job in the vocabulary the gate will judge. That is where S3 ends: a project that predates the contract now built to it, with the three things only the operator could have written still intact.

![Follow "View onboarding session" and watch it work](frames/12-follow-view-onboarding-session-and-watch.png)

<details><summary>What you should see</summary>

- `data-page` is `session`
- `data-page-ready` is `true`
- `data-session-kind` is `onboarding`
- `data-buildout-row-count` is `5`

</details>
