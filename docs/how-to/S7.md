---
kind: how-to
story: S7
generated_from: tests/stories/S7.story.mjs
---

# Create library components

> Generated by `npm run stories -- --story S7` from `tests/stories/S7.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open the Library

The Library is the parts bin: skills, hooks, connections, templates, and what the community has published. It is not a dashboard — every shelf on it is something an agent composes.

![Open the Library](frames/01-open-the-library.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `skills`

</details>

## 2. Press "+ New skill"

A skill is a reusable instruction packet — the unit an agent composes to know how to do something. This page offers two ways to make one, and the operator is about to take the one that does not involve typing markdown.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not click through to "/skills/new" from "/skills/new": locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('a[href="/skills/new"]').first()[22m
[2m    - locator resolved to <a class="btn btn-sm" href="/skills/new" data-action="new-skill">+ New skill</a>[22m
[2m  - attempting click action[22m
[2m    - waiting for element to be visible, enabled and stable[22m
[2m  - element was detached from the DOM, retrying[22m
. The control exists but was not actionable — obscured, disabled or detached.

![Press "+ New skill"](frames/02-press-new-skill.png)

<details><summary>What you should see</summary>

- `data-page` is `skill-builder`
- `data-page-ready` is `true`

</details>

## 3. Describe the skill to the creation agent instead of writing the package by hand

This is the fork in the road, and both branches are real: the operator can type the package into the form beside this one, or describe what they want and let the creation agent draft it. Same library, same scan, same approval — different amount of typing.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/authoring/<authoringSessionId>" needs <authoringSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Describe the skill to the creation agent instead of writing the package by hand](frames/03-describe-the-skill-to-the-creation-agent.png)

## 4. Read what it drafted, give the package its directory name, and approve it

The agent drafts; the operator names and approves. Nothing an agent wrote enters the library on the agent’s own say-so, and the id the operator types is the directory it lands in.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not fill [data-field="session-package-id"] with "story-s7-skill": locator.evaluate: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-field="session-package-id"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Read what it drafted, give the package its directory name, and approve it](frames/04-read-what-it-drafted-give-the-package-it.png)

<details><summary>What you should see</summary>

- `data-page` is `skill-builder`
- `data-page-ready` is `true`

</details>

## 5. Go back to the Library

The Library is where the operator returns between kinds. Each shelf is a different sort of part, and the next one is the only sort that is executable code.

![Go back to the Library](frames/05-go-back-to-the-library.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `hooks`

</details>

## 6. Press "+ New hook"

A hook is the other kind of part: not instructions but a script, run on an agent’s lifecycle event. It is the only thing in the library that is executable code, which is why it is the only thing with a security gate in front of it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not click through to "/hooks/new" from "/hooks/new": locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('a[href="/hooks/new"]').first()[22m
[2m    - locator resolved to <a href="/hooks/new" class="btn btn-sm" data-action="new-hook">+ New hook</a>[22m
[2m  - attempting click action[22m
[2m    - waiting for element to be visible, enabled and stable[22m
[2m    - element is not stable[22m
[2m  - retrying click action[22m
[2m    - waiting for element to be visible, enabled and stable[22m
[2m  - element was detached from the DOM, retrying[22m
. The control exists but was not actionable — obscured, disabled or detached.

![Press "+ New hook"](frames/06-press-new-hook.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-builder`
- `data-page-ready` is `true`

</details>

## 7. Write the hook by hand: what it runs on, what it matches, what it does, and what it may touch

The permissions fields are the operator declaring, up front, what this script is allowed to reach. They are also what the scanner checks the script against — a behaviour the manifest declares is still counted, because the declaration is written by whoever wrote the script.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not fill [data-field="hook-permissions-network"] with "": locator.fill: Error: Input of type "checkbox" cannot be filled
Call log:
[2m  - waiting for locator('[data-field="hook-permissions-network"]').first()[22m
[2m    - locator resolved to <input type="checkbox" data-field="hook-permissions-network"/>[22m
[2m    - fill("")[22m
[2m  - attempting fill action[22m
[2m    - waiting for element to be visible, enabled and editable[22m
. The control is absent, disabled, obscured or not yet rendered.

![Write the hook by hand: what it runs on, what it matches, what it does, and what it may touch](frames/07-write-the-hook-by-hand-what-it-runs-on-w.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-builder`
- `data-page-ready` is `true`

</details>

## 8. Read the security scan before trusting it

This is the gate that exists because a hook runs with the operator’s own credentials. The scan is static, it is shown before the approval and not after, and a declared behaviour is downgraded but still counted — never hidden.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/hooks/story-s7-hook" from "/hooks/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Read the security scan before trusting it](frames/08-read-the-security-scan-before-trusting-i.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-builder`

</details>

## 9. Check what is actually in the package — every file, and the fingerprint over all of them

Approving bytes you were never shown is not approving. The page lists every file in the package with its own hash and the fingerprint over the set, so a sibling script the entry point quietly sources cannot ride along unseen.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/hooks/story-s7-hook" from "/hooks/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Check what is actually in the package — every file, and the fingerprint over all of them](frames/09-check-what-is-actually-in-the-package-ev.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-builder`

</details>

## 10. Approve the hook

Trust and verdict are two different axes, and this is the trust one. Approved and runnable is the state that lets an agent carry it; until the operator pressed this, the hook was a file on disk that nothing would ever execute.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="approve-hook"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="approve-hook"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Approve the hook](frames/10-approve-the-hook.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-builder`

</details>

## 11. Go back to the Library once more

Third kind, same parts bin.

![Go back to the Library once more](frames/11-go-back-to-the-library-once-more.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `templates`

</details>

## 12. Press "+ New template" and write the template the hook’s output is filed into

A template is one markdown definition file — the shape an artifact comes out in. It is the least dramatic thing in the library and the one that decides whether two runs produce comparable output.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not fill [data-field="template-category"] with "studio/artifact-templates": locator.selectOption: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-field="template-category"]').first()[22m
[2m    - locator resolved to <select id="tpl-category" data-field="template-category">…</select>[22m
[2m  - attempting select option action[22m
[2m    2 × waiting for element to be visible and enabled[22m
[2m      - did not find some options[22m
[2m    - retrying select option action[22m
[2m    - waiting 20ms[22m
[2m    2 × waiting for element to be visible and enabled[22m
[2m      - did not find some options[22m
[2m    - retrying select option action[22m
[2m      - waiting 100ms[22m
[2m    10 × waiting for element to be visible and enabled[22m
[2m       - did not find some options[22m
[2m     - retrying select option action[22m
[2m       - waiting 500ms[22m
. The control is absent, disabled, obscured or not yet rendered.

![Press "+ New template" and write the template the hook’s output is filed into](frames/12-press-new-template-and-write-the-templat.png)

<details><summary>What you should see</summary>

- `data-page` is `template-builder`
- `data-page-ready` is `true`

</details>

## 13. Draft the project’s own working instructions, so the parts have a house style to follow

Instructions are the fourth kind of component and the only one that is about the project rather than about forge: the AGENTS.md every agent dispatched at this repo reads before it does anything.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/instructions/<instructionsSessionId>" needs <instructionsSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Draft the project’s own working instructions, so the parts have a house style to follow](frames/13-draft-the-project-s-own-working-instruct.png)

## 14. Open an agent from the Agents pillar and bind the hook to it

A hook is inert until an agent carries it. Binding is the act that makes a library part part of a worker, and it is the reason the hook’s own page counts how many agents carry it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - no real-nav path to "/agents/brain-ingest" from "/templates/new": no [data-nav] pillar and no link points at it. The runner does not fall back to page.goto — an unreachable route must not pass as a beat.

![Open an agent from the Agents pillar and bind the hook to it](frames/14-open-an-agent-from-the-agents-pillar-and.png)

<details><summary>What you should see</summary>

- `data-page` is `template-builder`

</details>

## 15. Run the agent, and watch the hook fire on the session ending

This is where S7 ends, and it ends on a claim the product does not yet make: that the hook the operator wrote, scanned, approved and bound actually RAN. A hook nobody can prove fired is a hook nobody should trust, and the whole gate in front of it — the scan, the package fingerprint, the approval — is spent guarding an event with no record.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="run-agent"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="run-agent"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Run the agent, and watch the hook fire on the session ending](frames/15-run-the-agent-and-watch-the-hook-fire-on.png)

<details><summary>What you should see</summary>

- `data-page` is `template-builder`

</details>
