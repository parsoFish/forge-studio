---
kind: how-to
story: S7
generated_from: tests/stories/S7.story.mjs
---

# Create library components

> Generated by `npm run stories -- --story S7` from `tests/stories/S7.story.mjs`. Do not hand-edit — edit the story and re-run it.

## 1. Open the Library

The Library is the parts bin: skills, hooks, connections, templates, and what the community has published. It is not a dashboard — every shelf on it is something an agent composes.

![Open the Library](frames/01-open-the-library.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `skills`

</details>

## 2. Press "+ New skill"

A skill is a reusable instruction packet — the unit an agent composes to know how to do something. This page offers two ways to make one, and the operator is about to take the one that does not involve typing markdown.

![Press "+ New skill"](frames/02-press-new-skill.png)

<details><summary>What you should see</summary>

- `data-page` is `skill-builder`
- `data-page-ready` is `true`
- `data-section` is `skill-new`

</details>

## 3. Describe the skill to the creation agent instead of writing the package by hand

This is the fork in the road, and both branches are real: the operator can type the package into the form beside this one, or describe what they want and let the creation agent draft it. Same library, same scan, same approval — different amount of typing.

![Describe the skill to the creation agent instead of writing the package by hand](frames/03-describe-the-skill-to-the-creation-agent.png)

<details><summary>What you should see</summary>

- `data-minted-session-id` is `2026-09-12T04-41-51-a505bb70`

</details>

## 4. Open the authoring session it just started

Forge mints the session and says so on the page the operator is already standing on, rather than moving them somewhere they did not ask to go. Reaching it is the operator’s own next act.

![Open the authoring session it just started](frames/04-open-the-authoring-session-it-just-start.png)

<details><summary>What you should see</summary>

- `data-page` is `session`
- `data-page-ready` is `true`
- `data-session-kind` is `authoring`
- `data-session-phase` is `analyzing`

</details>

## 5. Read what it drafted, give the package its directory name, and approve it

The agent drafts; the operator names and approves. Nothing an agent wrote enters the library on the agent’s own say-so, and the id the operator types is the directory it lands in — as a draft, which is not the same as trusted.

![Read what it drafted, give the package its directory name, and approve it](frames/05-read-what-it-drafted-give-the-package-it.png)

<details><summary>What you should see</summary>

- `data-page` is `skill-detail`
- `data-skill-id` is `story-s7-skill`
- `data-page-ready` is `true`
- `data-skill-trust` is `draft`

</details>

## 6. Trust it: approve the draft in the library

Two approvals, because they answer different questions. The first says the draft is finished; this one says you trust it enough to let an agent load it. Until this press the package is installed, quarantined and invisible to every palette — which is what a trust gate is for.

![Trust it: approve the draft in the library](frames/06-trust-it-approve-the-draft-in-the-librar.png)

<details><summary>What you should see</summary>

- `data-page` is `skill-detail`
- `data-skill-id` is `story-s7-skill`
- `data-page-ready` is `true`
- `data-skill-trust` is `ready`

</details>

## 7. Go back to the Library

The Library is where the operator returns between kinds. Each shelf is a different sort of part, and the next one is the only sort that is executable code.

![Go back to the Library](frames/07-go-back-to-the-library.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `hooks`

</details>

## 8. Press "+ New hook"

A hook is the other kind of part: not instructions but a script, run on an agent’s lifecycle event. It is the only thing in the library that is executable code, which is why it is the only thing with a security gate in front of it.

![Press "+ New hook"](frames/08-press-new-hook.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-builder`
- `data-page-ready` is `true`
- `data-section` is `hook-new`

</details>

## 9. Write the hook by hand: what it runs on, what it does, and what it may touch

The permissions fields are the operator declaring, up front, what this script is allowed to reach. They are also what the scanner checks the script against — a behaviour the manifest declares is still counted, because the declaration is written by whoever wrote the script.

![Write the hook by hand: what it runs on, what it does, and what it may touch](frames/09-write-the-hook-by-hand-what-it-runs-on-w.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-detail`
- `data-hook-id` is `story-s7-hook`
- `data-page-ready` is `true`
- `data-hook-event` is `SessionEnd`

</details>

## 10. Read the security scan before trusting it

This is the gate that exists because a hook runs with the operator’s own credentials. The scan is static, it is shown before the approval and not after, and a declared behaviour is downgraded but still counted — never hidden.

![Read the security scan before trusting it](frames/10-read-the-security-scan-before-trusting-i.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-detail`
- `data-hook-id` is `story-s7-hook`
- `data-scan-verdict` is `clean`
- `data-finding-count` is `0`
- `data-critical-count` is `0`

</details>

## 11. Check what is actually in the package — every file, and the fingerprint over all of them

Approving bytes you were never shown is not approving. The page lists every file in the package with its own hash and the fingerprint over the set, so a sibling script the entry point quietly sources cannot ride along unseen.

![Check what is actually in the package — every file, and the fingerprint over all of them](frames/11-check-what-is-actually-in-the-package-ev.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-detail`
- `data-hook-id` is `story-s7-hook`
- `data-package-hash` is `sha256:ae605d2524973232eea650baf308a968f44cb3cd1d21e37d49fcaf74d714439d`
- `data-package-file-count` is `2`

</details>

## 12. Approve the hook

Trust and verdict are two different axes, and this is the trust one. Approved and runnable is the state that lets an agent carry it; until the operator pressed this, the hook was a file on disk that nothing would ever execute.

![Approve the hook](frames/12-approve-the-hook.png)

<details><summary>What you should see</summary>

- `data-page` is `hook-detail`
- `data-hook-id` is `story-s7-hook`
- `data-hook-trust` is `approved`
- `data-hook-runnable` is `true`

</details>

## 13. Go back to the Library once more

Third kind, same parts bin.

![Go back to the Library once more](frames/13-go-back-to-the-library-once-more.png)

<details><summary>What you should see</summary>

- `data-page` is `library`
- `data-page-ready` is `true`
- `data-section` is `templates`

</details>

## 14. Press "+ New template" and write the template the hook’s output is filed into

A template is one markdown definition file — the shape an artifact comes out in. It is the least dramatic thing in the library and the one that decides whether two runs produce comparable output.

![Press "+ New template" and write the template the hook’s output is filed into](frames/14-press-new-template-and-write-the-templat.png)

<details><summary>What you should see</summary>

- `data-page` is `template-detail`
- `data-page-ready` is `true`

</details>

## 15. Head for the Agents pillar

Instructions are the fourth kind of part, and the only door to them is through Sessions — which is not a pillar. Getting there is three clicks from the parts bin.

![Head for the Agents pillar](frames/15-head-for-the-agents-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `agents-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 16. Follow the Sessions entry

Every session forge has ever run is here, and so is the only list of the kinds it can start.

![Follow the Sessions entry](frames/16-follow-the-sessions-entry.png)

<details><summary>What you should see</summary>

- `data-page` is `sessions-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 17. Open the instructions launcher

Three hops from the parts bin to the door for the fourth kind of part, and this is the third: the launcher itself. The story walks it rather than teleporting, because an operator cannot teleport.

![Open the instructions launcher](frames/17-open-the-instructions-launcher.png)

<details><summary>What you should see</summary>

- `data-page` is `session-kickoff`
- `data-page-ready` is `true`

</details>

## 18. Draft the project’s own working instructions, so the parts have a house style to follow

Instructions are the fourth kind of component and the only one that is about the project rather than about forge: the AGENTS.md every agent dispatched at this repo reads before it does anything.

![Draft the project’s own working instructions, so the parts have a house style to follow](frames/18-draft-the-project-s-own-working-instruct.png)

<details><summary>What you should see</summary>

- `data-minted-session-id` is `2026-09-12T04-42-56-20946267`

</details>

## 19. Open the instructions session it just started

Same shape as the skill: forge mints the session and says so where the operator already is, and reaching it is their own next act.

![Open the instructions session it just started](frames/19-open-the-instructions-session-it-just-st.png)

<details><summary>What you should see</summary>

- `data-page` is `session`
- `data-page-ready` is `true`
- `data-session-kind` is `instructions`

</details>

## 20. Head back to the Agents pillar

The parts are all made now — a skill, a hook, a template, a house style. None of them does anything yet. Binding happens on a worker’s own page, and the only way back to a worker is the pillar the operator started from.

![Head back to the Agents pillar](frames/20-head-back-to-the-agents-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `agents-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 21. Open brain-ingest from the roster

The roster is a list of workers; binding happens on one worker’s own page. This is the hop the operator makes without thinking about it, and the one the story forgot to write down.

![Open brain-ingest from the roster](frames/21-open-brain-ingest-from-the-roster.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-page-ready` is `true`
- `data-agent-id` is `brain-ingest`

</details>

## 22. Bind the hook to the agent, and save it

A hook is inert until an agent carries it. Binding is the act that makes a library part part of a worker, and it is the reason the hook’s own page counts how many agents carry it.

![Bind the hook to the agent, and save it](frames/22-bind-the-hook-to-the-agent-and-save-it.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `brain-ingest`
- `data-accepts` is `hook`
- `data-count` is `1`

</details>

## 23. Run the agent, and watch the hook fire on the session ending

This is where S7 ends, and it ends on a claim the product does not yet make: that the hook the operator wrote, scanned, approved and bound actually RAN. A hook nobody can prove fired is a hook nobody should trust, and the whole gate in front of it — the scan, the package fingerprint, the approval — is spent guarding an event with no record.

![Run the agent, and watch the hook fire on the session ending](frames/23-run-the-agent-and-watch-the-hook-fire-on.png)

<details><summary>What you should see</summary>

- `data-page` is `agents`
- `data-agent-id` is `brain-ingest`
- `data-run-status` is `running`
- `data-run-id` is `_agent-brain-ingest-2026-09-12T04-42-58-649-14px`

</details>
