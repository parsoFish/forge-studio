---
kind: how-to
story: S6
generated_from: tests/stories/S6.story.mjs
---

# Create a new knowledge base

> Generated by `npm run stories -- --story S6` from `tests/stories/S6.story.mjs`. Do not hand-edit — edit the story and re-run it.

> **This story did not pass.** The steps marked RED below describe what the operator flow is meant to do, not what it currently does.

## 1. Open Studio on the Knowledge pillar

Knowledge is where everything forge has learned lives, as graphs it can read back. The knowledge base this story is about is not here yet — the Flow it belongs to has been running without one.

![Open Studio on the Knowledge pillar](frames/01-open-studio-on-the-knowledge-pillar.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 2. Press "+ New KB"

One form, and the only two things that matter on it are what this knowledge base is called and what it is bound to. A knowledge base with no binding is a folder.

![Press "+ New KB"](frames/02-press-new-kb.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge-new`
- `data-page-ready` is `true`
- `data-section` is `kb-new`

</details>

## 3. Name it, bind it to the forge-develop flow, scope it to the review-band, and create it

Binding is the act. A band scope says WHICH readers on that Flow this knowledge is for — review-band means the reviewer, not the planner and not the developer — so the knowledge lands in front of the agent it was written for and nowhere else. Forge writes the graph with one index node and nothing in it.

![Name it, bind it to the forge-develop flow, scope it to the review-band, and create it](frames/03-name-it-bind-it-to-the-forge-develop-flo.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-kb-id` is `story-s6`
- `data-node-count` is `1`

</details>

## 4. Follow the seeding session forge started for it

Creating a knowledge base starts an agent that fills it. Forge does not leave the operator with an empty graph and a shrug — it offers the session it already started, and the operator goes and watches it.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/project-brain/<seedSessionId>" needs <seedSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Follow the seeding session forge started for it](frames/04-follow-the-seeding-session-forge-started.png)

## 5. Brief the seeding agent on what this knowledge base is for, and let it read

A band-scoped knowledge base has no project repo to read — it reads the Flow’s own archived cycles, and synthesises what review kept finding. The operator’s brief is the only thing that tells it which of those findings are worth keeping.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/project-brain/<seedSessionId>" needs <seedSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Brief the seeding agent on what this knowledge base is for, and let it read](frames/05-brief-the-seeding-agent-on-what-this-kno.png)

## 6. Read the themes it drafted and approve them into the knowledge base

Nothing an agent drafts enters the brain without a human saying yes. This is the gate: the operator reads what it wrote, and only then does it become knowledge the next run will act on.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/project-brain/<seedSessionId>" needs <seedSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Read the themes it drafted and approve them into the knowledge base](frames/06-read-the-themes-it-drafted-and-approve-t.png)

## 7. Back on the knowledge base, find a real theme in the graph

Seeded means there is something in it. One theme with a name the operator can read is the difference between a knowledge base and an empty promise.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-theme-node: expected "<seedTheme>", absent from the page
> - data-theme-active: expected "false", absent from the page

![Back on the knowledge base, find a real theme in the graph](frames/07-back-on-the-knowledge-base-find-a-real-t.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 8. Go to the Projects pillar to find the project on the other end of that binding

A knowledge base is bound to a Flow, and a Flow runs against projects. To prove the binding does anything, the operator has to go to one of them.

![Go to the Projects pillar to find the project on the other end of that binding](frames/08-go-to-the-projects-pillar-to-find-the-pr.png)

<details><summary>What you should see</summary>

- `data-page` is `projects-index`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 9. Open mdtoc

mdtoc is the project this repo ships with, so it is the one a clean checkout can plan against.

![Open mdtoc](frames/09-open-mdtoc.png)

<details><summary>What you should see</summary>

- `data-page` is `projects`
- `data-project-id` is `mdtoc`
- `data-page-ready` is `true`

</details>

## 10. Press "Architect →" to plan a piece of work on it

A knowledge base earns its keep at planning time. So the operator starts a real planner run against the project on the other end of that binding, and gives it a piece of work to think about.

![Press "Architect →" to plan a piece of work on it](frames/10-press-architect-to-plan-a-piece-of-work-.png)

<details><summary>What you should see</summary>

- `data-page` is `architect-new`
- `data-page-ready` is `true`
- `data-section` is `new-idea`
- `data-roster-state` is `ok`

</details>

## 11. Describe the work, cap what this run may spend, and press "Start architect"

The planner is a real agent and a real agent costs money, so the operator caps this run before starting it. What happens next is the thing S6 exists to prove.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - route "/sessions/architect/<architectSessionId>" needs <architectSessionId>, which no earlier beat bound. A beat binds a segment by expecting `<name>` for a data-* key the product mints.

![Describe the work, cap what this run may spend, and press "Start architect"](frames/11-describe-the-work-cap-what-this-run-may-.png)

## 12. Go back to the knowledge base and see that the planner actually read it — the brain-index event, naming this KB and what it took

This is the beat the whole product rests on. Forge’s claim is that it compounds — that what one run learned changes what the next one plans — and the only honest evidence for that is the planner’s own read, on the record, naming this knowledge base. Until that is on the page, compounding is something forge asserts about itself rather than something the operator can check.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-kb-id: expected "story-s6", got "cycles"

![Go back to the knowledge base and see that the planner actually read it — the brain-index event, naming this KB and what it took](frames/12-go-back-to-the-knowledge-base-and-see-th.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`
- `data-kb-id` is `cycles`

</details>

## 13. Open the knowledge base’s Health tab and drain it to green

A seeded knowledge base is not a healthy one. The drain runs every lint finding to a fixed point — frontmatter, index sync, dead links, orphans — and it is the operator pressing one button rather than the operator editing markdown.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - could not press [data-action="drain-to-green"]: locator.click: Timeout 5000ms exceeded.
Call log:
[2m  - waiting for locator('[data-action="drain-to-green"]').first()[22m
. The control is absent, disabled, obscured or not yet rendered.

![Open the knowledge base’s Health tab and drain it to green](frames/13-open-the-knowledge-base-s-health-tab-and.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>

## 14. Read the diff the drain proposes, file by file, before accepting it

This is where S6 ends: a rendered diff, per file, of what an agent changed inside the operator’s knowledge — not a claim that it cleared N findings. The drain reports what it PROPOSED and what a post-fix lint says actually cleared, and those are different facts on purpose.

> **This step is RED — not verified working.** The run asserted state the product did not show:
>
> - data-drain-proposal-file: expected "<drainedFile>", absent from the page
> - data-drain-proposal-disposition: expected "applied", absent from the page

![Read the diff the drain proposes, file by file, before accepting it](frames/14-read-the-diff-the-drain-proposes-file-by.png)

<details><summary>What you should see</summary>

- `data-page` is `knowledge`
- `data-page-ready` is `true`
- `data-fetch-status` is `ok`

</details>
