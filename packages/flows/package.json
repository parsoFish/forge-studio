{
  "name": "@forge/flows",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The flow runner against a PhaseExecutor port, scheduler, daemon, queue state machine, manifest, triggers, run model, git/PR/work-item mechanics.",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"**/*.test.ts\""
  }
}
