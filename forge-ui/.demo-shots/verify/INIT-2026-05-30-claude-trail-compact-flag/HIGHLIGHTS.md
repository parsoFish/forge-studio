# verify:cycle — decision-point highlights (2026-06-08)

A **real** forge cycle on `claude-harness` (initiative `INIT-2026-05-30-claude-trail-compact-flag`,
routine tier, reset to its pre-cycle base) driven by `scripts/verify-cycle.mjs … --send-back`.
Frames are in [`./frames/`](./frames/); the recorded video is [`./video → cycle.webm`](./cycle.webm);
machine summary in [`./summary.json`](./summary.json).

## The build this proves (MVUS req b)

The review demo now **foregrounds the assessed intent + per-acceptance-criterion evaluated output**
(met / partial / missed + evidence). This run **proves it end-to-end**: the *live* unifier authored
**7 `acEvaluations`** (all `met`, one per AC) into the merged `demo.json` — not seeded, generated by
the real agent. See **`frames/18-decision-review-evaluation.png`** (the `data-section="demo-evaluation"`
section) and the merged `projects/claude-harness/demo/.../demo.json`.

## The four decision points you asked to highlight

| # | Highlight | Where | Real? |
|---|-----------|-------|-------|
| 1 | **Conversational architect** (idea → interview → plan → replan) | e2e gallery: `../../e2e/frames/03-step02-architect-explores.png`, `04-step03-architect-questions.png`, `05-step04-operator-answers.png`, `07-step05-council-plan.png`, `09-step06b-replan.png` | UI flow (real frontend, emulated runner) — see note A |
| 2 | **PM breaks up work** | `frames/10-decision-pm-work-items.png` + `11-project-manager-complete.png` | **Real** (live PM decomposed the initiative into work items) |
| 3 | **Cost + tokens as work is done** | `frames/09-decision-cost-and-tokens.png` + `12/13-developer-loop-*.png` | **Real** (WI-1 dev: $1.03; live per-phase cost rollup) |
| 4 | **Review + a pass back to the unifier** | `frames/18-decision-review-evaluation.png` → `19-decision-send-back.png` → `20-decision-re-review.png`; clean loop in e2e `../../e2e/frames/08-step06-send-back.png` | **Mechanism real** — see note B |

## Verdict (this run)

- ✓ dev-loop completed N/N work items · ✓ project tests green · ✓ cost **$6.36 / $40** · **merged → done → reflection ran**
- The build is proven (7 real `acEvaluations`); the cycle merged and reflected.

## Honest notes

**A — Architect is captured from the e2e gallery, not the verify run.** The architect is the
interactive *pre-cycle* phase; `forge serve` (what verify drives) starts at the PM. Forcing a
real-LLM architect into the capability gate adds cost + fragility for no autonomous-capability gain,
so the conversational-architect frames come from the e2e operator-journey (real UI). The architect's
*real* capability is exercised separately when you start a session from the dashboard.

**B — The send-back was real but its unifier re-run hit a re-run-only conflict.** The verdict POST was
accepted (HTTP 200; `appendedUnifierItems: [UWI-2, UWI-3]`) and the drain fired — the
review→unifier hand-back **mechanism works**. The unifier's *re-execution* then failed at
`rebase onto origin/main` because this is a **routine re-run of an already-merged corpus feature**
(origin/main already contains compact-flag, so the re-implemented branch conflicts). A greenfield
cycle (feature not yet on main) would not hit this. The clean, complete
send-back → unifier → re-review → approve loop is shown in the e2e gallery
(`../../e2e/frames/08-step06-send-back.png` → `…/23-step12c-rerun` → `…/24-step12d-re-review`).

**Harness fixes made this round:** `verify-cycle.mjs` now (1) strips the stale `cycle_id` on stage and
(2) clears prior-run residue before serving, so it tracks the right cycle; and (3) the gate accepts the
manifest landing in `_queue/done/` as the authoritative merge signal (the bridge status read `null`
post-merge even on cleanly-merged cycles).
