# {{TITLE}}

{{NORTH_STAR}}

A dependency-light TypeScript HTTP API scaffolded by forge (R4-03 `typescript-api`
starter).

```bash
npm install
npm test          # fast unit gate over pure routing logic
npm run build     # tsc -> dist/
npm start         # serve on PORT (default 3000)
curl localhost:3000/health
```

See `AGENTS.md` for the agent contract and `roadmap.md` for the architecture.
