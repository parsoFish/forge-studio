# {{TITLE}}

{{NORTH_STAR}}

A dependency-light TypeScript CLI scaffolded by forge (R4-03 `typescript-cli`
starter).

```bash
npm install
npm test          # fast unit gate
npm run build     # tsc -> dist/
node dist/index.js <name>
```

See `AGENTS.md` for the agent contract and `roadmap.md` for the architecture.
