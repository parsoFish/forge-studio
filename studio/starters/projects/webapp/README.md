# {{TITLE}}

{{NORTH_STAR}}

A dependency-light TypeScript web UI scaffolded by forge (the `webapp`
starter).

```bash
npm install
npm test          # fast unit gate over pure page-rendering logic
npm run build     # tsc -> dist/
npm start         # serve on PORT (default 3000)
curl localhost:3000/
```

See `AGENTS.md` for the agent contract and `roadmap.md` for the architecture.
