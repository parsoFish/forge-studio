# {{TITLE}} — Roadmap

> {{NORTH_STAR}}

Machine-readable architecture context for the architect/PM (C4). Replace the
stubs below with the real initiatives as the north star is decomposed.

## Architecture

- **Shape:** a dependency-light TypeScript CLI. `src/` holds small pure functions;
  `dist/` is the compiled binary the acceptance gate runs.
- **Gates:** `npm test` (fast unit) per iteration; `npm run acceptance` (built
  binary vs fixture) once per cycle.

## Initiatives

- [ ] INIT-1 — TODO: the first slice of {{NORTH_STAR}}
