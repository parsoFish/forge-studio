# `cli/` — Operator utilities + the UI bridge (Scope 1)

> **Scope 1 — framework.** The `forge` subcommand handlers, operator utilities, and the
> Studio **bridge** (`bridge-studio.ts`) the UI talks to. The CLI must not reach into
> orchestrator internals; the UI reaches the orchestrator only through this bridge.

See [docs/repo-map.md](../docs/repo-map.md) · the committed CLI reference at
[docs/reference/cli.md](../docs/reference/cli.md).
