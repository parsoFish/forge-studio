# env-optimiser — Patterns

- [`specify-driven-features`](./themes/specify-driven-features.md) — Features land via `specs/<NNN>-<slug>/` with spec.md, plan.md, tasks.md, quickstart.md.
- [`redaction-before-storage`](./themes/redaction-before-storage.md) — Mandatory secret filtering before any SQLite write; new collectors must invoke the redaction engine.
