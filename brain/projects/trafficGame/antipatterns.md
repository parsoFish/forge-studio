# trafficGame — Antipatterns

> Project-specific failure modes. `brain-lint` ensures every theme page with `category: antipattern` (project-scoped) appears here exactly once.

- [`algorithm-heavy-items`](./themes/algorithm-heavy-items.md) — Steiner topology + graph colouring as single work items caused 48% failure rate. Decompose before queueing.
