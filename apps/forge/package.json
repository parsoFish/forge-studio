{
  "name": "@forge/app-forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The CLI router and bridge host: origin/CSRF/JSON envelope, WS, health, daemon wiring. Hard cap 800 lines.",
  "exports": {
    ".": "./index.ts",
    "./*": "./*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"**/*.test.ts\""
  }
}
