# slugifier

Tiny TypeScript library: convert any string to a URL-safe slug.

## Quality gate

```
npm test
```

This is an empty seed; the developer-loop populates `src/` and `tests/` from the initiative manifest.
