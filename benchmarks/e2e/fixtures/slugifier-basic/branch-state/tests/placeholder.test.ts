/**
 * Placeholder test so `npm test` (`node --test tests/`) doesn't fail when
 * the project has no real tests yet. The dev-loop adds the actual test file
 * during its work; this one stays as a scaffold marker.
 */
import { test } from 'node:test';

test('scaffold placeholder — replace with real tests', () => {
  // intentionally empty
});
