[
  {
    "id": "slugifier-basic",
    "project": "slugifier",
    "seed_tree": "fixtures/slugifier-basic/branch-state",
    "manifest_path": "fixtures/slugifier-basic/manifest.md",
    "initiative_id": "INIT-2026-05-09-slugifier-basic",
    "spec": {
      "manifest_ac_command": ["npm", "test", "--silent"],
      "non_functional_checks": [
        {
          "description": "edge case: empty input handled in tests",
          "command": ["bash", "-c", "grep -ri -E '(empty|\\\"\\\"|return \\\"\\\")' tests/ >/dev/null"]
        },
        {
          "description": "edge case: emoji or non-Latin scripts handled in tests",
          "command": ["bash", "-c", "grep -ri -E '(emoji|non.?latin|unicode|cyrillic|chinese|🎉|你)' tests/ >/dev/null"]
        },
        {
          "description": "implementation collapses consecutive separators",
          "command": ["bash", "-c", "grep -ri -E '(consecutive|repeated|collapse|/-+/|/-{2,}/|/[^a-z0-9]+/)' src/ >/dev/null"]
        },
        {
          "description": "implementation normalises Latin accents",
          "command": ["bash", "-c", "grep -ri -E '(normalize|nfd|nfkd|diacritic|accent|combining)' src/ >/dev/null"]
        }
      ],
      "required_pr_signals": ["why", "edge case", "demo"]
    },
    "expected": {
      "max_rounds": 2,
      "max_cost_usd": 12,
      "pre_existing_tests_cmd": ["bash", "-c", "[ -f tests/slugify.test.ts ] && npm test --silent || true"]
    }
  }
]
