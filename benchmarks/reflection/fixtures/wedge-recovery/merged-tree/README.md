# trafficGame — distributeFlow

Merged sample tree for the reflection bench fixture `wedge-recovery`.

`distributeFlow(volume, weights)` returns a vector of per-lane volumes
proportional to the supplied weights.
