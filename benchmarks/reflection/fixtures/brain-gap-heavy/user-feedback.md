# User feedback — brain-gap-heavy

## Answers

- The four brain-query gaps cluster into two themes:
  - **Convention gaps** (GAP-001, GAP-003): the brain doesn't yet
    document env-optimiser's CLI flag conventions. Worth a brain page on
    the project's flag-list style.
  - **Demo-format gaps** (GAP-004): the brain doesn't yet have a forge-wide
    decision on demo-tape format for property-style helpers. The current
    convention emerged ad-hoc; document it.
  - **Behaviour-spec gaps** (GAP-002): equal-sign-form is in the AC, but
    the brain didn't have prior precedent. Now it does (this initiative).
- Resolving these gaps in the reflection pass closes the loop: every gap
  the brain couldn't answer at query time gets a theme that fills it.

## Free-form

Multiple gap-id references in the retro is the right pattern — each gap
should appear in the retro doc OR as a `## Sources` reference in a
gap-resolving theme. The bench rubric checks this.
