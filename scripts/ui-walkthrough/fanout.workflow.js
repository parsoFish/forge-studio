export const meta = {
  name: 'ui-walkthrough-fanout',
  description: 'Per-pillar operator-style UI exploration of Forge Studio, then adversarial verification of each cluster\'s findings',
  phases: [
    { title: 'Explore', detail: 'one opus explorer per UI cluster, playwright + source', model: 'opus' },
    { title: 'Verify', detail: 'one sonnet verifier per cluster refutes/confirms S1+S2 findings', model: 'sonnet' },
  ],
}

const OUT = (args && args.outDir) || '_walkthrough'
const BRIEF = (args && args.brief) || 'scripts/ui-walkthrough/EXPLORER-BRIEF.md'
const MODEL_EXPLORE = (args && args.exploreModel) || 'opus'
const MODEL_VERIFY = (args && args.verifyModel) || 'sonnet'
const ONLY = (args && args.only) || null  // optional array of cluster keys

const CLUSTERS = [
  { key: 'home-sessions', title: 'Home + Sessions index + session cancel/cleanup', routes: ['/', '/sessions'], extra: `
Focus: operator notes 2 and 3. On Home: is it obvious which strip is SESSIONS vs KNOWLEDGE BASES needing attention? What do the "attention" cards actually link to and does each link land somewhere actionable? What do data-status=retrying / failed rows on Home mean — hover/click each; find where "retrying" comes from in source. Recent-activity ledger rows: click every row kind. On /sessions: every row, every filter/sort, every "needs you" chip. Establish: is there ANY cancel/abandon/dismiss/archive control for a session anywhere (row, detail page, kickoff page)? Read cli/ui-bridge.ts session routes + forge-ui/app/sessions/** + components for abandon endpoints (demo-builder/abandon, project-brain/abandon exist — are they wired for other kinds?). Enumerate the 22 sessions from GET /api/studio/sessions and classify each bad-state one (phase, age, needsYou, why stuck, which files on disk under _logs/ or projects/*/forge/ back it) — that classification is a deliverable in your notes. Also check the WS reconnect banner behaviour (open a page, observe [data-ws]/connection indicators).` },
  { key: 'projects', title: 'Projects index + project detail (30+ buttons) + showcase + onboarding + /architect/new', routes: ['/projects', '/projects/<id>', '/projects/<id>/showcase', '/projects/new', '/architect/new?project=<id>'], extra: `
Every project card; on each project detail page click EVERY button/tab/expander (there are ~30-37) in an inventory-driven loop, capturing what each did (request, url change, body change, dialog). Note /projects/new renders the project-detail page for id "new" (dead route from a Home/projects CTA?) — find the link source. gitpulse: contract-stages 409, trafficgame: contract-stages 404 + onboarding/active 404 — explain from source. Showcase pages: mdtoc showcase 404s demo.json — what does the UI show for it? Try the "New project"/onboard path up to (not including) anything that creates git remotes; creating a throwaway local project dir under projects/ via the UI is acceptable ONLY if the UI offers it (record what it creates; use name w7-throwaway). /architect/new: what does the kickoff form offer vs /sessions/architect/new — duplicated surfaces? Where does the project's roadmap/initiative history link into flows/runs/artifacts (loop closure)?` },
  { key: 'flows', title: 'Flows index + per-flow monitor/build + run detail pages + scheduler + stuck betterado plan', routes: ['/flows', '/flows/<id>', '/flows/new', '/flows/<id>/run/<runId>'], extra: `
Operator note 4 is yours: architect session 2026-08-14T15-26-59-072e0775 (terraform-provider-betterado) is "committed"; _queue/pending/INIT-2026-08-14-betterado-gap-registry.md exists; GET /api/scheduler/status → running:false. Trace the full intended loop in source (architect commit → queue → scheduler → forge-develop run → verdict → merge → reflect) and identify EXACTLY where the UI fails to offer the next step (is there a Start-scheduler / Run-now control anywhere? /flows/forge-develop monitor? project page? Home?). The Home page links /flows/forge-architect/run/INIT-2026-08-14-betterado-gap-registry — what does that render? Do NOT press scheduler start / develop start (they spawn a real develop cycle that pushes branches) — but record what the controls claim and read the handlers. Flow run detail pages never set data-page-ready and every one 404s review-findings.json — explain. Links to /flows/unknown/run/... and /flows/release-refine/... (flow ids that no longer exist) — where do they come from and what should happen? Flow BUILD tab: try authoring a throwaway flow (name w7-throwaway-flow) end to end incl. save; record what persists (studio/flows/?) and whether it can be deleted; if it cannot be deleted from UI, note it and leave the file for the orchestrator to remove. (W7-C1 retired the vestigial reflect + onboard flow wrappers — reflection is a standalone post-merge agent run, onboarding is a session; their monitors no longer exist, so any lingering link to them should render the retired/unknown NotFound.) Note 7's "two new flow buttons on the KB screen" are in the knowledge cluster, but check whether the flows index/monitor also shows them.` },
  { key: 'agents', title: 'Agents index + agent detail (57 buttons) + agent run pages + agent builder + run history', routes: ['/agents', '/agents/<slug>', '/agents/new', '/agents/<slug>/run/<runId>'], extra: `
Inventory-driven click of every button/tab on 3 representative agent detail pages (developer-ralph, community-refresh, reflector) + /agents/new. Every agent page requests [bridge]/api/events/ (empty id) → 404 — find the source. Try a real agent run through the UI on the cheapest tier for a cheap read-only agent (e.g. contract-check or brain-lint style agent if offered; NOT developer-ralph, NOT release-finalizer, NOT reflector (writes brain)) — record the run page behaviour, live log, thinking drawer, outputs section (bead forge-75j says typed-outputs has no data source), history. Agent builder: create throwaway agent "w7-throwaway-agent" via the UI if it lets you, record what it writes (studio/agents/? skills/?), whether it can be edited/deleted from UI. Also check the "recent runs" widget on the agents page (operator wants the same on the KB screen) — describe its data source (route + component file) so it can be reused.` },
  { key: 'library', title: 'Library hub + Skills/Hooks/Connections/Templates shelves + builders + detail pages', routes: ['/library', '/skills', '/skills/<id>', '/skills/new', '/hooks', '/hooks/<id>', '/hooks/new', '/connections', '/connections/<id>', '/templates', '/templates/<id>'], extra: `
Operator notes 8 and 9. Library page: enumerate the shelf rows/cards; find the Knowledge-bases card (to be removed) and note its component/file. For each shelf: can the operator CREATE and EDIT an item in Studio? Templates: is there any create/edit path at all? Skills/hooks builders (/skills/new, /hooks/new): drive them end-to-end with throwaway names (w7-throwaway-skill / w7-throwaway-hook) — record what persists on disk and whether edit/delete exist afterwards. Note the "authoring" session kind (/sessions/authoring/new) — how does it relate to these builders (duplicate surface?). /skills/agent-browser: page loads but GET /api/studio/skills/agent-browser 404 (community-source skill listed in the library but no detail) — explain. Connection detail: what does "install" do for MCPs — record the claim, do not install anything network-fetching. Detail pages: is every "used by"/"carried by" link live?` },
  { key: 'community', title: 'Community browser + hubs + item detail + install + community-refresh session', routes: ['/community', '/community/<kind>/<id>', '/sessions/community-refresh/new', '/sessions/community-refresh/<id>'], extra: `
Operator note 5 is yours. Map the whole community surface: hubs list, sorts/filters/freshness, item detail, install (do NOT install anything that runs a network fetch/npm install; installing a plain skill file copy from the local registry cache is acceptable if that is what install does — read cli/ui-bridge.ts install handler first and record what it does). Read studio/community/registry.yaml (+ studio/community/staging/ which is untracked in the tree — what is it? leftover of the refresh agent?) and skills/community-refresh/SKILL.md. There is an existing community-refresh session 2026-08-18T12-54-32-abdfd26b in phase "gathering" needsYou=true — open it and describe what the operator sees, whether it produced anything, and what "gathering" means. Then START ONE community-refresh session yourself on the cheapest tier via /sessions/community-refresh/new and follow it for up to ~15 minutes (poll the session page and GET /api/studio/sessions; read its event log under _logs/ or wherever the spine writes; find via source) — record exactly what it does, what it produces (a draft registry? where?), and whether the UI ever reflects it. Assess against the operator's desired capability: refresh from sources AND targeted "find me skills for X" search — what would it take (which files)? Also inventory how many items the registry has vs how many the UI shows, and any hard-coded hubs.` },
  { key: 'knowledge', title: 'Knowledge index + KB detail tabs + health tab + drain-to-green (REAL RUN) + new KB + kb-cleanup', routes: ['/knowledge', '/knowledge?id=<kb>', '/knowledge?id=<kb>&tab=health', '/knowledge/new', '/sessions/kb-cleanup/new', '/sessions/kb-cleanup/<id>'], extra: `
Operator notes 6 and 7 are yours. Inventory EVERY button on the health tab for kb=cycles (there are many): for each, record label, what it claims, the request it fires, whether it is gated while another job runs, and overlap with others. Build a table of them (deliverable in notes). Then run drain-to-green for REAL on ONE small KB (pick the smallest with lint issues — probably gitpulse or mdtoc, NOT cycles/forge-dev/betterado which are large) and observe for up to ~15 min: what the UI shows while running (any log? progress? states?), what the bridge job writes (find via cli/ui-bridge.ts + orchestrator: kb drain job, event log path), what the final state is, and whether the lint numbers actually change. Record every second the operator would be looking at a screen that says nothing. Note the KB id case issue: /api/studio/kbs/trafficGame → 400 while the id in the KBs list is trafficGame — explain from source (SLUG regex?). Identify the "two new flow buttons" on the KB screen (probably kb-bound flow triggers) — what they do, why they look wrong. Find where the agents page's "recent runs" widget lives (component + data route) and specify how to reuse it here. Also /knowledge/new: create a throwaway KB "w7-throwaway-kb" if the form allows; record what it writes and whether it can be deleted from the UI (leave it for the orchestrator to remove). Existing kb-cleanup sessions (2 in "drafting" for .kb-cycles): open, describe state, whether Approve works (bead forge-vxg says approve 400s) — do not approve if it would apply edits to brain/, just record.` },
  { key: 'sessions-kinds', title: 'Generic session surface — every kind: kickoff pages + existing sessions + activity drawer + verdict controls', routes: ['/sessions/<kind>/new for architect, demo, instructions, kb-cleanup, community-refresh, project-brain, authoring, onboarding', '/sessions/<kind>/<id> for each existing session'], extra: `
For EACH kind's kickoff page: inventory the form (project picker, tier picker, inputs), submit with a small realistic input on the cheapest tier for architect (project demo-project, idea "add a --version flag that prints the package version"), instructions (demo-project), demo (gitpulse, on the failed cycle if it asks), project-brain (gitpulse) — kick off ONE of each of those four; skip onboarding kickoff if it requires a new repo path (record the form instead); community-refresh and kb-cleanup kickoffs belong to other clusters (do NOT start those). Follow each started session for up to ~15 minutes: does the page live-update, does the ActivityLog bottom drawer show thinking/tool events, do the affordance buttons (answer question, approve, revise, abandon…) appear at the right phase, is there a cancel? Answer interview questions with short answers to move architect forward through draft → council → select → manifest; STOP before any control that commits/queues (record what it says). Then open EVERY pre-existing session listed by GET /api/studio/sessions (esp. demo sessions from 2026-06-25 and 2026-08-14 in "briefing" with 404 event logs, instructions awaiting-verdict, architect drafting r6-06 fixture) and record: what renders, which controls appear, which work, error text. Note the demo session pages fire GET /api/events/_demo-<id> → 404. Note architect sessions render 0 buttons for committed ones — is there a link to the resulting initiative/queue/run? (loop closure, operator note 4).` },
  { key: 'artifact-plan', title: 'Artifact page + plan approval (operator note 1) + review comments + verdict endpoints', routes: ['/artifact', '/artifact?cycle=<cycleId>', 'plan artifact for the betterado architect session'], extra: `
Operator note 1 is yours: find every place a PLAN can be approved (the /artifact page bottom Approve button + bottom banner Approve; the architect session page; anything on the project page). Read forge-ui/app/artifact/page.tsx + components + cli/ui-bridge.ts /api/plan-verdict + /api/verdict + /api/architect/* handlers. Reproduce: open the plan artifact for the committed betterado architect session (2026-08-14T15-26-59-072e0775) and for gitpulse 2026-08-03T01-09-32; press each Approve control; capture request/response (or absence of a request), console, and body change. Determine WHY nothing happens (precondition gate? session shape mismatch pre/post wave-6? disabled without reason? handler no-op?). Then check the same for a fresh session shape if the sessions-kinds explorer has produced one by the time you look (GET /api/studio/sessions, kind architect, newest). Also exercise /artifact for a done develop cycle (2026-07-11T17-26-34_INIT-2026-07-11-cli-sort-flag) and the failed one (2026-08-03T01-16-00_INIT-2026-08-03-init-coupling-change-coupling-command): every tab/section/button; review-comments controls; verdict controls (do NOT press approve/merge on a real run — record); the /api/artifact/<cycle>/review-findings.json 404s. Empty /artifact (no cycle): what renders?` },
  { key: 'crosscut', title: 'Cross-cutting: nav, deep links, WS reconnect, error/empty states, bridge-down behaviour, unknown ids, a11y basics', routes: ['all — sample each pillar'], extra: `
Do not duplicate the per-pillar clusters' click-everything; instead: (a) run through the crawl.json unvisited list (82 routes at the bottom of crawl-summary.txt) and visit each, recording issues; (b) unknown/garbage ids on every dynamic route (/projects/nope, /flows/nope, /agents/nope, /skills/nope, /hooks/nope, /connections/nope, /templates/nope, /community/skill/nope, /sessions/architect/nope, /sessions/bogus-kind/new, /flows/forge-develop/run/nope, /knowledge?id=nope, /artifact?cycle=nope) — do they render honest not-found states or fake pages?; (c) nav active-pillar correctness on every route family (data-nav active class), back/forward navigation, breadcrumb links; (d) the WS indicator: what does the UI show if the bridge is unreachable — do NOT stop the bridge; instead point a page at FORGE_BRIDGE_URL override if the UI supports it, or open the UI with the bridge blocked via a playwright route() intercept that aborts localhost:4123 requests — record each page's degraded state (blank? spinner forever? honest banner?); (e) tab title / document.title per route; (f) keyboard: Tab through Home and one detail page, are focus states visible; obvious contrast/overflow problems at 1280x800 and 1920x1080 viewports (screenshots); (g) copy scan: 'undefined', 'NaN', '[object Object]', 'null', raw ids where names should be, timestamps in ISO where relative expected; (h) any route in forge-ui/app not reachable from any link (orphan pages) — compare app/ tree vs crawl.json links.` },
]

const FINDINGS = {
  type: 'object',
  properties: {
    cluster: { type: 'string' },
    routesExplored: { type: 'array', items: { type: 'string' } },
    controlsTried: { type: 'number' },
    sessionsStarted: { type: 'array', items: { type: 'object', properties: { kind: { type: 'string' }, sessionId: { type: 'string' } } } },
    findingsFile: { type: 'string' },
    findingsCount: { type: 'object', properties: { S1: { type: 'number' }, S2: { type: 'number' }, S3: { type: 'number' } } },
    topFindings: { type: 'array', items: { type: 'object', properties: { id: { type: 'string' }, severity: { type: 'string' }, title: { type: 'string' } }, required: ['id', 'severity', 'title'] } },
    unexplored: { type: 'array', items: { type: 'string' } },
    notes: { type: 'string' },
  },
  required: ['cluster', 'routesExplored', 'findingsFile', 'findingsCount', 'topFindings', 'unexplored', 'notes'],
}

const VERIFY = {
  type: 'object',
  properties: {
    cluster: { type: 'string' },
    verdicts: { type: 'array', items: { type: 'object', properties: {
      id: { type: 'string' }, verdict: { type: 'string', enum: ['CONFIRMED', 'REFUTED', 'DOWNGRADED', 'UPGRADED', 'DUPLICATE'] },
      severity: { type: 'string' }, note: { type: 'string' }, rootCause: { type: 'string' }, source: { type: 'array', items: { type: 'string' } } },
      required: ['id', 'verdict', 'note'] } },
    additionalFindings: { type: 'array', items: { type: 'object', properties: { id: { type: 'string' }, severity: { type: 'string' }, title: { type: 'string' }, route: { type: 'string' } } } },
    notes: { type: 'string' },
  },
  required: ['cluster', 'verdicts', 'notes'],
}

const explorePrompt = (c) => `You are the wave-7 UI explorer for cluster "${c.key}" — ${c.title}.
Working directory: /home/parso/forge. FIRST read ${BRIEF} in full and follow it exactly (ground rules, finding schema, return shape). Then read scripts/ui-walkthrough/lib.mjs.
Your routes: ${c.routes.join(' · ')}.
Cluster-specific brief:${c.extra}
Write findings to ${OUT}/findings/${c.key}.jsonl (ids ${c.key}-01, -02, …), scripts under ${OUT}/explore/${c.key}/, screenshots under ${OUT}/explore/${c.key}/shots/. Read source to explain root causes (file:line). Be exhaustive: keep going until every control on every route in your cluster has been tried in every reachable state; when a path is blocked, record it under unexplored with the reason. Your final message must be the JSON return object described in the brief.`

const verifyPrompt = (c, r) => `You are the adversarial verifier for wave-7 UI-walkthrough cluster "${c.key}" — ${c.title}.
Working directory: /home/parso/forge. Read ${BRIEF} (rules: Studio already running on 4123/4124, never restart it or run ui:journey, only write under ${OUT}/), then read ${OUT}/findings/${c.key}.jsonl.
Explorer summary: ${JSON.stringify(r).slice(0, 4000)}
For EVERY S1 and S2 finding: try to REFUTE it. Reproduce it yourself (playwright via scripts/ui-walkthrough/lib.mjs — write scripts under ${OUT}/explore/${c.key}-verify/; or curl the bridge; or read the source lines cited). Default to REFUTED only if you actually reproduce the opposite; CONFIRMED needs your own repro or a source-level proof; DOWNGRADE/UPGRADE severity when the explorer mis-rated; mark DUPLICATE when two ids are the same defect (say which id survives). Fix root causes / file:line where the explorer's is missing or wrong. S3 findings: skim, only comment if clearly wrong. Do not start new sessions or long jobs; read-only + navigation only. Append your verdicts as JSONL lines to ${OUT}/findings/${c.key}.verify.jsonl ({id, verdict, severity, note, rootCause, source}). If while verifying you find a NEW defect the explorer missed, add it to additionalFindings AND append it as a normal finding line (id ${c.key}-V01…) to ${OUT}/findings/${c.key}.jsonl. Return the JSON described by the schema.`

phase('Explore')
const results = await pipeline(
  ONLY ? CLUSTERS.filter((c) => ONLY.includes(c.key)) : CLUSTERS,
  (c) => agent(explorePrompt(c), { label: `explore:${c.key}`, phase: 'Explore', model: MODEL_EXPLORE, schema: FINDINGS })
          .then((r) => { log(`explored ${c.key}: ${r ? JSON.stringify(r.findingsCount) : 'null'}`); return r }),
  (r, c) => r ? agent(verifyPrompt(c, r), { label: `verify:${c.key}`, phase: 'Verify', model: MODEL_VERIFY, schema: VERIFY })
          .then((v) => { log(`verified ${c.key}: ${v ? v.verdicts.length + ' verdicts' : 'null'}`); return { explore: r, verify: v } })
          : null,
)
return { clusters: results.filter(Boolean).map((x) => ({ key: x.explore.cluster, counts: x.explore.findingsCount, sessions: x.explore.sessionsStarted, unexplored: x.explore.unexplored, verdicts: x.verify?.verdicts?.length ?? null, extra: x.verify?.additionalFindings ?? [] })), nulls: results.filter((x) => !x).length }
