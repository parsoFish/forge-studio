# Cleanup Plan — KB `forge-dev`

Session: `2026-08-14T08-34-38-a34fff82`
Generated by: `brain-maintenance` (drafting phase)
Findings input: 2 findings (1 `length.soft-cap`, 1 `edge.dangling`)

---

## Summary

Two findings require attention. Neither is structurally blocking, but both
should be resolved before the next reflection cycle to keep the KB tidy.

---

## Actions

### 1. Soft-cap overage — `brain-read-policy.md`

The theme currently runs 65 body lines against the 60-line soft cap. The file
has two inline `>` blockquote callouts (R1-01, R1-06, lines 82–84) that
summarise guard-test references, plus a `## Sources` section (lines 86–89)
whose two bullets are near-duplicate references to the same raw cycle archive.
Condensing those areas — merging the two similar source bullets, and folding
the R1 callouts into a compact "History" subsection — should recover the
headroom without losing any substance.

- [length.soft-cap] brain/forge-dev/themes/brain-read-policy.md — Condense the theme from 65 to ≤60 body lines by merging the two near-duplicate source bullets and folding the R1-01 / R1-06 blockquote callouts into a compact inline history note.

---

### 2. Dangling edge — `exploration-vs-implementation-initiatives.md`

The `related_themes` entry `forge-current-architecture-as-built` points at a
slug that no longer exists anywhere under `brain/**/themes/`. The KB log
(`brain/forge-dev/log.md`, line 794) confirms the theme was once ingested as
`forge/themes/forge-current-architecture-as-built.md` (reference category),
but no file matching that slug — with or without a date prefix — exists in
the current worktree. The target is genuinely gone; no plausible survivor was
found.

Accordingly, the action is to drop the dangling entry outright rather than
repoint it. The theme body already covers the same orientation ground via the
`[[holistic-metrics-onboarding]]`, `[[parametric-design-search]]`, and
`[[human-directed-work-as-initiatives]]` wikilinks, so nothing architecturally
significant is lost.

- [edge.dangling] brain/forge-dev/themes/exploration-vs-implementation-initiatives.md — Drop the `related_themes` entry `forge-current-architecture-as-built` because no matching theme file exists anywhere under `brain/**/themes/` and no plausible surviving slug was found.

---

## What this plan does NOT do

- No new content is ingested.
- No theme file is created, edited, or deleted here — this is a draft only.
- No finding was invented beyond the two in `status.json`.
- The `brain-read-policy.md` condense action deliberately avoids proposing
  any specific wording changes; the `brain-fix` agent will author those.
